---
phase: 03-public-listings
plan: 06
type: execute
wave: 5
depends_on:
  - 03-05
files_modified:
  - canary-propos/src/app/(manager)/dashboard/page.tsx
  - canary-propos/src/app/(manager)/inquiries/page.tsx
autonomous: true
requirements:
  - LIST-07

must_haves:
  truths:
    - "Manager dashboard shows an Inquiries count card with the number of 'new' status inquiries for their org"
    - "Manager can navigate to /inquiries (or an inquiries section) and see a list of all inquiries/applications"
    - "Inquiry list shows: visitor name, email, type (inquiry/application), listing title, date, status"
    - "Manager can update inquiry status (new → contacted → closed) from the list"
  artifacts:
    - path: "canary-propos/src/app/(manager)/dashboard/page.tsx"
      provides: "Dashboard updated with new Inquiries count card"
    - path: "canary-propos/src/app/(manager)/inquiries/page.tsx"
      provides: "Manager inquiries list page with status update actions"
  key_links:
    - from: "dashboard/page.tsx"
      to: "inquiries table"
      via: "count query filtered by org_id and status='new'"
    - from: "inquiries/page.tsx"
      to: "inquiries table"
      via: "SELECT with listings JOIN for title, ordered by created_at DESC"
---

<objective>
Surface new inquiries to the manager: a count card on the dashboard and a dedicated /inquiries list page where the manager can review and update inquiry statuses.

Purpose: Completes LIST-07 in-app notification requirement (D-12). Manager sees inquiry activity without real-time subscriptions — server-rendered count on next page load.
Output: Updated dashboard page with inquiry count card, new /inquiries manager page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@canary-propos/src/types/supabase.ts

<interfaces>
From existing dashboard page (pattern — read the file before editing):
  Uses createClient() (authenticated)
  Fetches data for the caller's org via callerPerson.org_id
  Renders summary cards in a grid (likely div.grid with card components)

Inquiry count query:
  const { count } = await supabase
    .from('inquiries')
    .select('*', { count: 'exact', head: true })
    .eq('org_id', callerPerson.org_id)
    .eq('status', 'new')

Inquiry list query (for /inquiries page):
  supabase.from('inquiries')
    .select('id, name, email, phone, type, status, created_at, move_in_date, budget, note,
             listings!listing_id(id, listing_title, unit_id,
               units!unit_id(properties!property_id(street_address, city)))')
    .eq('org_id', callerPerson.org_id)
    .order('created_at', { ascending: false })

Status update server action (add to src/app/actions/inquiries.ts):
  updateInquiryStatus(id: string, status: 'new' | 'contacted' | 'closed'): Promise<{ error?: string }>
  Uses authenticated createClient(), guards with org_id, revalidates '/inquiries'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add inquiry count card to manager dashboard</name>
  <files>
    canary-propos/src/app/(manager)/dashboard/page.tsx,
    canary-propos/src/app/actions/inquiries.ts
  </files>
  <read_first>
    - canary-propos/src/app/(manager)/dashboard/page.tsx (existing summary card pattern)
    - canary-propos/src/app/actions/inquiries.ts (add updateInquiryStatus here)
  </read_first>
  <action>
    Edit canary-propos/src/app/(manager)/dashboard/page.tsx:
    - After the existing data fetches, add: const { count: newInquiryCount } = await supabase.from('inquiries').select('*', { count: 'exact', head: true }).eq('org_id', callerPerson.org_id).eq('status', 'new')
    - Add an "Inquiries" summary card to the dashboard card grid using the same card pattern as existing cards. Show: count value (default 0 if null), label "New inquiries", link href="/inquiries"
    - Badge style: if count > 0 use amber background (to draw attention), if 0 use stone/grey

    Edit canary-propos/src/app/actions/inquiries.ts:
    - Add updateInquiryStatus(id: string, status: 'new' | 'contacted' | 'closed') server action
    - Validate id is a UUID, status is one of the three values
    - Update using authenticated createClient(), guard with .eq('org_id', orgId)
    - Call revalidatePath('/inquiries') and revalidatePath('/dashboard')
    - Return { error?: string }
  </action>
  <verify>
    <automated>cd /c/Users/aaron/OneDrive/Claude\ Code/canary-propos && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>dashboard/page.tsx compiles with new inquiry count card. updateInquiryStatus action exported from inquiries.ts. Card shows new inquiry count and links to /inquiries.</done>
</task>

<task type="auto">
  <name>Task 2: Create manager inquiries list page</name>
  <files>canary-propos/src/app/(manager)/inquiries/page.tsx</files>
  <read_first>
    - canary-propos/src/app/(manager)/properties/page.tsx or similar list page (pattern)
    - canary-propos/src/app/actions/inquiries.ts (updateInquiryStatus — just added)
    - canary-propos/src/types/supabase.ts (Tables['inquiries'])
  </read_first>
  <action>
    Create canary-propos/src/app/(manager)/inquiries/page.tsx as an async RSC:
    - Verify session (same pattern as other manager pages: createClient, getUser, redirect if null, fetch callerPerson)
    - Fetch inquiries with the join described in context interfaces, ordered by created_at DESC
    - Render a page header "Inquiries" with a count of total inquiries

    Desktop table columns: Name, Email, Type (badge: "Showing request" for inquiry, "Application" for application), Listing, Date, Status (Select or buttons), Actions
    - Status column: render a small form with three buttons (New / Contacted / Closed) or a select dropdown that submits updateInquiryStatus. Use the form action pattern: action={updateInquiryStatus.bind(null, inquiry.id, newStatus)}.
    - Type badge: inquiry = stone, application = amber
    - Status badge: new = green, contacted = blue, closed = stone
    - Listing column: show listing_title and property address

    Mobile cards: each card shows name, email, type badge, listing title, date, and status update buttons stacked vertically.

    Empty state: "No inquiries yet. Once visitors submit inquiries from your listings page, they'll appear here."

    Add a link "← Dashboard" at the top.

    The page is already protected by middleware (it's in the (manager) route group under /inquiries which hits isProtectedPath).

    Note: add '/inquiries' to the isProtectedPath function in middleware.ts if it isn't already covered. Check: middleware.ts currently covers /dashboard, /people, /settings but not /inquiries explicitly. Since /inquiries starts with a new prefix, add it. (This is a one-line change to middleware.ts; include it in files_modified if needed, or note it as a follow-up — the (manager) layout provides a secondary server-side check.)
  </action>
  <verify>
    <automated>cd /c/Users/aaron/OneDrive/Claude\ Code/canary-propos && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>inquiries/page.tsx compiles. Renders inquiry list with status badges and update controls. Links back to dashboard. Empty state present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manager → updateInquiryStatus | Authenticated action must guard by org_id to prevent cross-org status mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-17 | Tampering | updateInquiryStatus | mitigate | Action fetches org_id from authenticated user's people row; UPDATE guarded with .eq('org_id', orgId) — cannot update another org's inquiry |
| T-03-18 | Information Disclosure | inquiries list page | mitigate | Page uses authenticated createClient() + org_id filter; RLS double-enforces at DB layer |
</threat_model>

<verification>
- npx tsc --noEmit passes
- Dashboard page renders with "New inquiries" count card linking to /inquiries
- /inquiries page loads and shows inquiry list when inquiries exist
- Status update buttons/select change inquiry.status and revalidate the page
- /inquiries redirects to /login when visited without auth
</verification>

<success_criteria>
Manager sees new inquiry count on dashboard and can review/update all inquiries from /inquiries. Phase 3 feature complete: listings published, browsable publicly, inquiries captured, manager notified.
</success_criteria>

<output>
Create `.planning/phases/03-public-listings/03-06-SUMMARY.md` when done
</output>
