---
phase: 03-public-listings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - canary-propos/supabase/migrations/0014_create_listings.sql
  - canary-propos/supabase/migrations/0015_create_inquiries.sql
  - canary-propos/src/types/supabase.ts
autonomous: false
requirements:
  - LIST-01
  - LIST-02
  - LIST-03
  - LIST-04
  - LIST-05
  - LIST-06
  - LIST-07

must_haves:
  truths:
    - "listings table exists with all D-01/D-02 fields and status enum (draft/published/unlisted)"
    - "inquiries table exists with all D-10 fields and type enum (inquiry/application)"
    - "RLS: managers have full CRUD on listings/inquiries within their org"
    - "RLS: anon role can SELECT listings where status = published"
    - "RLS: anon role can INSERT into inquiries (no SELECT)"
    - "supabase.ts types reflect both new tables"
    - "check-rls.ts passes with no unlocked tables"
  artifacts:
    - path: "canary-propos/supabase/migrations/0014_create_listings.sql"
      provides: "listings table DDL + RLS"
    - path: "canary-propos/supabase/migrations/0015_create_inquiries.sql"
      provides: "inquiries table DDL + RLS"
    - path: "canary-propos/src/types/supabase.ts"
      provides: "Updated TypeScript types including Tables['listings'] and Tables['inquiries']"
  key_links:
    - from: "listings.unit_id"
      to: "units.id"
      via: "FK with UNIQUE constraint (one listing per unit)"
    - from: "listings.org_id"
      to: "organizations.id"
      via: "RLS scoping"
    - from: "inquiries.listing_id"
      to: "listings.id"
      via: "FK"
---

<objective>
Write and push two Supabase migrations: listings table (per D-01, D-02, D-03) and inquiries table (per D-09, D-10). Push to the linked project, regenerate TypeScript types, and run the RLS linter.

Purpose: Everything in Phase 3 depends on these tables existing in the database with correct RLS before any UI is written.
Output: Two migration SQL files, updated supabase.ts, clean RLS lint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-data-model/02-01-SUMMARY.md
@canary-propos/supabase/migrations/0008_create_properties.sql
@canary-propos/supabase/migrations/0010_extend_units.sql
@canary-propos/src/types/supabase.ts

<interfaces>
From canary-propos/supabase/migrations/0008_create_properties.sql (RLS pattern to follow):
- Every table gets org_id UUID NOT NULL REFERENCES organizations(id)
- RLS enabled immediately after CREATE TABLE
- Manager policy: using (org_id = (jwt_org_id())) pattern
- Helper function jwt_org_id() is already defined in 0003_rls_helpers.sql

From 0010_extend_units.sql (enum pattern):
- CREATE TYPE ... AS ENUM (...) before CREATE TABLE
- Drop type on rollback if needed

From 02-01-SUMMARY.md:
- Last applied migration: 0013_update_storage_bucket.sql
- Next migration numbers: 0014, 0015
- units table has: id, property_id, unit_number, bedrooms, bathrooms, sq_footage, status, asking_rent, amenities text[], org_id
- people table has: id, user_id, org_id, role text[]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 0014_create_listings.sql migration</name>
  <files>canary-propos/supabase/migrations/0014_create_listings.sql</files>
  <read_first>
    - canary-propos/supabase/migrations/0003_rls_helpers.sql (jwt_org_id helper)
    - canary-propos/supabase/migrations/0008_create_properties.sql (RLS policy pattern)
    - canary-propos/supabase/migrations/0010_extend_units.sql (enum + column pattern)
  </read_first>
  <action>
    Create migration 0014_create_listings.sql. All work is done in this one SQL file.

    Step 1 — Create enum type: CREATE TYPE listing_status AS ENUM ('draft', 'published', 'unlisted');

    Step 2 — Create listings table with columns:
    - id UUID PRIMARY KEY DEFAULT gen_random_uuid()
    - org_id UUID NOT NULL REFERENCES organizations(id) ON DELETE CASCADE
    - unit_id UUID NOT NULL REFERENCES units(id) ON DELETE CASCADE
    - listing_title TEXT NOT NULL
    - listing_description TEXT
    - highlights TEXT[] DEFAULT '{}'
    - display_rent NUMERIC(10,2)
    - status listing_status NOT NULL DEFAULT 'draft'
    - available_from DATE
    - created_at TIMESTAMPTZ NOT NULL DEFAULT now()
    - updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
    - Add UNIQUE constraint on unit_id (one active listing per unit)

    Step 3 — Enable RLS: ALTER TABLE listings ENABLE ROW LEVEL SECURITY;

    Step 4 — RLS policies (per D-07):
    - Manager full access: CREATE POLICY "manager_listings_all" ON listings FOR ALL TO authenticated USING (org_id = jwt_org_id() AND (SELECT role FROM people WHERE user_id = auth.uid() LIMIT 1) && ARRAY['manager','admin','employee']::text[]) WITH CHECK (same);
    - Anon SELECT published only: CREATE POLICY "anon_listings_select" ON listings FOR SELECT TO anon USING (status = 'published');

    Step 5 — updated_at trigger: reuse the moddatetime or a simple trigger function that sets updated_at = now() on UPDATE. Define the trigger function inline if not already present in schema, then attach it to listings.

    Note on the manager RLS: use the role-check pattern from 0008_create_properties.sql exactly. Do not hand-roll a different pattern. If 0008 uses a simpler approach (e.g., checking people table inline), match it exactly.
  </action>
  <verify>
    <automated>cd /c/Users/aaron/OneDrive/Claude\ Code/canary-propos && npx supabase db lint --linked 2>&1 | head -20</automated>
  </verify>
  <done>0014_create_listings.sql exists with CREATE TYPE, CREATE TABLE (all D-01/D-02 columns), ENABLE ROW LEVEL SECURITY, two RLS policies (manager all + anon select published).</done>
</task>

<task type="auto">
  <name>Task 2: Write 0015_create_inquiries.sql migration</name>
  <files>canary-propos/supabase/migrations/0015_create_inquiries.sql</files>
  <read_first>
    - canary-propos/supabase/migrations/0014_create_listings.sql (just written — listings.id FK target)
    - canary-propos/supabase/migrations/0008_create_properties.sql (RLS policy pattern)
  </read_first>
  <action>
    Create migration 0015_create_inquiries.sql.

    Step 1 — Create enums:
    - CREATE TYPE inquiry_type AS ENUM ('inquiry', 'application');
    - CREATE TYPE inquiry_status AS ENUM ('new', 'contacted', 'closed');

    Step 2 — Create inquiries table with columns (per D-10):
    - id UUID PRIMARY KEY DEFAULT gen_random_uuid()
    - org_id UUID NOT NULL REFERENCES organizations(id) ON DELETE CASCADE
    - listing_id UUID NOT NULL REFERENCES listings(id) ON DELETE CASCADE
    - type inquiry_type NOT NULL DEFAULT 'inquiry'
    - name TEXT NOT NULL
    - email TEXT NOT NULL
    - phone TEXT
    - move_in_date DATE
    - budget NUMERIC(10,2)
    - note TEXT
    - status inquiry_status NOT NULL DEFAULT 'new'
    - created_at TIMESTAMPTZ NOT NULL DEFAULT now()

    Step 3 — Enable RLS: ALTER TABLE inquiries ENABLE ROW LEVEL SECURITY;

    Step 4 — RLS policies (per D-07):
    - Manager can SELECT/UPDATE within org: CREATE POLICY "manager_inquiries_read_update" ON inquiries FOR ALL TO authenticated USING (org_id = jwt_org_id() AND role-check matches manager/admin/employee pattern from 0008) WITH CHECK (same);
    - Anon INSERT only (no SELECT): CREATE POLICY "anon_inquiries_insert" ON inquiries FOR INSERT TO anon WITH CHECK (true);

    The anon INSERT policy uses WITH CHECK (true) because the org_id is resolved server-side from the slug lookup before the insert — the visitor cannot supply a fake org_id if the server action enforces it. Document this assumption in a SQL comment.
  </action>
  <verify>
    <automated>ls /c/Users/aaron/OneDrive/Claude\ Code/canary-propos/supabase/migrations/0015_create_inquiries.sql</automated>
  </verify>
  <done>0015_create_inquiries.sql exists with both enums, CREATE TABLE (all D-10 columns), RLS enabled, two policies (manager all + anon insert).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Push migrations, regen types, run RLS linter</name>
  <what-built>Two migration SQL files: 0014_create_listings.sql and 0015_create_inquiries.sql</what-built>
  <read_first>
    - canary-propos/.env.local (confirm SUPABASE_ACCESS_TOKEN is set)
  </read_first>
  <action>
    Run these three commands from the canary-propos/ directory in order:

    1. npx supabase db push
       Expected: "Applying migration 0014_create_listings.sql... ok" and "Applying migration 0015_create_inquiries.sql... ok"

    2. npx supabase gen types typescript --linked > src/types/supabase.ts
       Expected: supabase.ts updated, file size increases by ~100+ lines

    3. npx tsx scripts/check-rls.ts
       Expected: PASS — no tables reported without RLS

    If step 1 fails with an auth error, ensure SUPABASE_ACCESS_TOKEN is set in the shell: export SUPABASE_ACCESS_TOKEN=$(cat .env.local | grep SUPABASE_ACCESS_TOKEN | cut -d= -f2)
  </action>
  <how-to-verify>
    1. Run: cd canary-propos && npx supabase db push
    2. Confirm both migrations show "ok" in output
    3. Run: npx supabase gen types typescript --linked > src/types/supabase.ts
    4. Run: npx tsx scripts/check-rls.ts
    5. Confirm output ends with PASS
    6. Open src/types/supabase.ts and confirm Tables includes 'listings' and 'inquiries' keys
  </how-to-verify>
  <resume-signal>Type "pushed" when all three commands pass, or describe the error output</resume-signal>
  <done>listings and inquiries tables exist in production DB, supabase.ts has their types, RLS linter passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon → inquiries INSERT | Unauthenticated visitor supplies inquiry payload; org_id is supplied by the server, not the client |
| anon → listings SELECT | Visitor reads only status=published rows; unpublished listings are invisible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | listings RLS anon policy | mitigate | anon SELECT policy filters status = 'published' — draft/unlisted never returned |
| T-03-02 | Tampering | inquiries INSERT anon | mitigate | org_id set server-side in Server Action from slug lookup — visitor cannot supply arbitrary org_id |
| T-03-03 | Elevation of Privilege | manager RLS | mitigate | Manager policy checks role[] includes manager/admin/employee via people table join |
| T-03-SC | Tampering | npm/supabase CLI installs | accept | No new npm packages in this plan; only supabase CLI commands already in project |
</threat_model>

<verification>
- Both migration files are valid SQL with no syntax errors (supabase db lint passes)
- supabase db push applies 0014 and 0015 with no errors
- supabase.ts contains Tables['listings'] with fields: id, org_id, unit_id, listing_title, listing_description, highlights, display_rent, status, available_from, created_at, updated_at
- supabase.ts contains Tables['inquiries'] with fields: id, org_id, listing_id, type, name, email, phone, move_in_date, budget, note, status, created_at
- check-rls.ts exits with code 0
</verification>

<success_criteria>
Two migrations pushed successfully, supabase.ts updated with both new table types, RLS linter shows zero unlocked tables.
</success_criteria>

<output>
Create `.planning/phases/03-public-listings/03-01-SUMMARY.md` when done
</output>
