---
phase: 03-public-listings
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - canary-propos/src/app/(manager)/properties/[id]/page.tsx
  - canary-propos/src/components/listings/ListingForm.tsx
  - canary-propos/src/app/actions/listings.ts
autonomous: true
requirements:
  - LIST-06

must_haves:
  truths:
    - "Property detail page has a Listings tab (fourth tab) alongside Building Info, Units, Leases"
    - "Listings tab shows current listing for the property, or a Create Listing prompt if none exists"
    - "Manager can create a listing via a Dialog form with all D-02 fields"
    - "Manager can toggle status between draft/published/unlisted via a button"
    - "Form uses react-hook-form + zod, follows the existing AddUnitForm/AddLeaseForm pattern"
  artifacts:
    - path: "canary-propos/src/app/(manager)/properties/[id]/page.tsx"
      provides: "Properties detail page with Listings tab added"
    - path: "canary-propos/src/components/listings/ListingForm.tsx"
      provides: "Create/Edit listing Dialog form component"
    - path: "canary-propos/src/app/actions/listings.ts"
      provides: "createListing, updateListing, toggleListingStatus server actions"
  key_links:
    - from: "ListingForm"
      to: "actions/listings.ts createListing"
      via: "Server Action bound to form"
    - from: "Listings tab"
      to: "listings table"
      via: "Supabase query filtered by property's unit_ids"
---

<objective>
Add a Listings tab to the existing property detail page. Managers can create or edit a listing for any unit in the property, toggle publish status, and see the current listing state — all within the property detail context (per D-13).

Purpose: Managers need a way to create and manage listings. The property detail page is the right context (per D-13 locked decision).
Output: Listings tab on /properties/[id], ListingForm dialog, three server actions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@canary-propos/src/app/(manager)/properties/[id]/page.tsx
@canary-propos/src/types/supabase.ts

<interfaces>
From properties/[id]/page.tsx (existing patterns to follow exactly):
- callerPerson: { org_id, role } — already fetched, already available in scope
- isManager boolean — already computed
- Tabs component from '@/components/ui/tabs' — already imported
- AddUnitForm pattern: Dialog trigger button + Dialog content with react-hook-form inside
- All DB queries use: supabase.from('table').select(...).eq('org_id', callerPerson.org_id)

Server action pattern (follow src/app/actions/contacts.ts or equivalent):
- 'use server' at top
- createClient() for authenticated Supabase client
- Validate with zod schema
- Insert/update with .eq('org_id', org_id) guard
- Return { error?: string } | { data: ... }
- Call revalidatePath('/properties/[id]') after mutation

Listing fields (from D-02, now in supabase.ts after plan 03-01):
  listing_title: string (required)
  listing_description: string (optional)
  highlights: string[] (optional, chips/tags input or comma-separated)
  display_rent: number (optional, overrides unit.asking_rent)
  status: 'draft' | 'published' | 'unlisted' (default draft)
  available_from: date string (optional)
  unit_id: uuid (required — picker from property's units)
  org_id: uuid (from callerPerson.org_id)

Units already fetched in page.tsx as unitsList — pass to ListingForm as prop.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ListingForm component and listing server actions</name>
  <files>
    canary-propos/src/components/listings/ListingForm.tsx,
    canary-propos/src/app/actions/listings.ts
  </files>
  <read_first>
    - canary-propos/src/components/properties/AddUnitForm.tsx (Dialog + react-hook-form pattern)
    - canary-propos/src/app/actions/ (existing action pattern — pick any one action file)
    - canary-propos/src/types/supabase.ts (Tables['listings'] type after plan 03-01 push)
  </read_first>
  <action>
    Create canary-propos/src/app/actions/listings.ts with these three server actions:

    createListing(formData): validates with zod schema (listing_title required, unit_id required, status defaults to 'draft', display_rent is coerced to number or null, available_from is date string or null, highlights is string[] default []). Inserts into listings table with org_id from authenticated user's people row. Calls revalidatePath('/properties/[id]') — since we don't have the property ID in scope, revalidatePath('/properties', 'layout') or pass propertyId as a hidden field. Returns { error: string } on failure.

    updateListing(id, formData): same zod schema, updates by id with org_id guard (.eq('id', id).eq('org_id', orgId)). Returns { error: string } on failure.

    toggleListingStatus(id, newStatus): accepts newStatus as 'draft' | 'published' | 'unlisted'. Updates listings.status. Guards by org_id. Revalidates path.

    Create canary-propos/src/components/listings/ListingForm.tsx as a Client Component ('use client'):
    - Props: propertyId (string), orgId (string), units (Array of {id, unit_number, bedrooms, bathrooms}), existingListing (optional — the current listing if one exists)
    - If existingListing is provided, render the form pre-populated (edit mode)
    - Trigger: Button labeled "Create listing" (if no existing) or "Edit listing" (if existing)
    - Dialog with react-hook-form + zodResolver
    - Fields: unit_id (Select from units prop), listing_title (Input), listing_description (Textarea), display_rent (Input type=number, optional), available_from (Input type=date, optional), highlights (Input, comma-separated, transform to string[] on submit)
    - Submit calls createListing or updateListing server action
    - Show error message from action response if present
    - Close dialog on success (use useRouter().refresh() pattern)

    Reuse shadcn/ui components already in src/components/ui/: dialog, select, form, input, button, textarea, label.
  </action>
  <verify>
    <automated>cd /c/Users/aaron/OneDrive/Claude\ Code/canary-propos && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>ListingForm.tsx and listings.ts compile without errors. Three server actions exported. Form renders with all D-02 fields. Edit mode pre-populates from existingListing prop.</done>
</task>

<task type="auto">
  <name>Task 2: Add Listings tab to property detail page</name>
  <files>canary-propos/src/app/(manager)/properties/[id]/page.tsx</files>
  <read_first>
    - canary-propos/src/app/(manager)/properties/[id]/page.tsx (full file — already in context)
    - canary-propos/src/components/listings/ListingForm.tsx (just created)
    - canary-propos/src/app/actions/listings.ts (just created — toggleListingStatus)
  </read_first>
  <action>
    Edit src/app/(manager)/properties/[id]/page.tsx. Add the Listings tab.

    Step 1 — Add import: import { ListingForm } from '@/components/listings/ListingForm'

    Step 2 — After fetching leases, add a query for listings. Using unitIds (already computed), query:
    supabase.from('listings').select('id, unit_id, listing_title, status, display_rent, available_from, listing_description, highlights').in('unit_id', unitIds).eq('org_id', callerPerson.org_id)
    Assign result to listingsList (default []).

    Step 3 — Add a fourth TabsTrigger: value="listings", label "Listings {count > 0 ? (count) : ''}"

    Step 4 — Add TabsContent value="listings":
    - If isManager and listingsList.length === 0: show empty state with "No listing yet" and ListingForm in create mode
    - If listingsList.length > 0: render a card for each listing showing: listing_title, status badge (draft=stone, published=green, unlisted=amber), display_rent or "—", available_from or "—", and:
      - ListingForm in edit mode (existingListing prop)
      - A Publish/Unpublish toggle button that calls toggleListingStatus server action inline as a form action. If status is 'published', button reads "Unpublish" and sets status to 'unlisted'. If status is 'draft' or 'unlisted', button reads "Publish" and sets status to 'published'.

    The toggle button uses a small form element with a hidden input for id and newStatus, submitted via formAction binding to toggleListingStatus. Use the pattern: action={toggleListingStatus.bind(null, listing.id, nextStatus)}.

    Do not change any existing tabs or data fetches.
  </action>
  <verify>
    <automated>cd /c/Users/aaron/OneDrive/Claude\ Code/canary-propos && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>page.tsx compiles. Fourth Listings tab present in TabsList. TabsContent renders listing cards with ListingForm and status toggle. Empty state shown when no listings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manager → listings mutations | Server actions must re-verify org membership before insert/update |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Elevation of Privilege | createListing server action | mitigate | Action fetches org_id from authenticated user's people row (not from form data) — user cannot inject a foreign org_id |
| T-03-08 | Tampering | toggleListingStatus | mitigate | Status update uses .eq('org_id', orgId) guard — cannot toggle another org's listing |
| T-03-09 | Tampering | unit_id in listing form | mitigate | Server action validates that unit_id belongs to the manager's org before insert (cross-reference units table with org_id) |
</threat_model>

<verification>
- npx tsc --noEmit passes
- /properties/[id] page has four tabs: Building Info, Units, Leases, Listings
- Listings tab shows empty state with "Create listing" for properties with no listings
- ListingForm dialog has: unit selector, title, description, display_rent, available_from, highlights
- Publish/Unpublish toggle button present on each listing card
- All three server actions compile and have org_id guards
</verification>

<success_criteria>
Manager can visit /properties/[id], click the Listings tab, create a listing via the dialog, and toggle its published status. TypeScript compiles cleanly.
</success_criteria>

<output>
Create `.planning/phases/03-public-listings/03-03-SUMMARY.md` when done
</output>
