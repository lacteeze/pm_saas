---
phase: 02-core-data-model
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - canary-propos/src/app/(manager)/people/page.tsx
  - canary-propos/src/components/people/ContactsTab.tsx
  - canary-propos/src/components/people/AddContactForm.tsx
  - canary-propos/src/components/people/EditContactForm.tsx
  - canary-propos/src/app/actions/contacts.ts
autonomous: true
requirements:
  - PEOPLE-01
  - PEOPLE-02
  - PEOPLE-03
  - PEOPLE-04

must_haves:
  truths:
    - "Manager sees two tabs on /people: Team (existing content) and Contacts (new)"
    - "Manager can create a contact with first name, last name, email, phone, role(s) (tenant/owner/vendor)"
    - "Contacts list shows name, email, phone, role badges, edit/deactivate actions"
    - "Manager can edit a contact's details via pre-filled dialog"
    - "Manager can deactivate a contact (sets active=false, removes from list)"
    - "Role display uses role.join(', ') — people.role is now text[] after 0012 migration"
    - "isManager check uses .includes() not === for role comparison"
    - "AddContactForm does NOT offer 'admin' as a role option (tenant/owner/vendor only)"
  artifacts:
    - path: "canary-propos/src/components/people/ContactsTab.tsx"
      provides: "Contacts table (desktop) + mobile cards + empty state"
      exports: ["ContactsTab"]
    - path: "canary-propos/src/components/people/AddContactForm.tsx"
      provides: "Dialog-based contact creation form"
      exports: ["AddContactForm"]
    - path: "canary-propos/src/components/people/EditContactForm.tsx"
      provides: "Pre-filled dialog for editing a contact + deactivate action"
      exports: ["EditContactForm"]
    - path: "canary-propos/src/app/actions/contacts.ts"
      provides: "createContact, updateContact, deactivateContact server actions"
      exports: ["createContact", "updateContact", "deactivateContact", "ActionResult"]
  key_links:
    - from: "canary-propos/src/app/(manager)/people/page.tsx"
      to: "ContactsTab"
      via: "TabsContent contacts imports ContactsTab, passes contacts array and isManager"
      pattern: "ContactsTab.*contacts.*isManager"
    - from: "canary-propos/src/components/people/AddContactForm.tsx"
      to: "canary-propos/src/app/actions/contacts.ts"
      via: "startTransition calling createContact()"
      pattern: "createContact"
---

<objective>
Add the Contacts tab to the /people page and implement full CRUD for contact records (tenants, owners, vendors). Updates people/page.tsx to use shadcn Tabs and fixes the role=== comparisons broken by the 0012 text[] migration.

Purpose: Satisfies PEOPLE-01 through PEOPLE-04. Creates the contact directory that the lease and property pages will reference.

Output: /people page with two working tabs; contacts can be created, edited, and deactivated.
</objective>

<execution_context>
@C:/Users/aaron/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/aaron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/aaron/OneDrive/Claude Code/.planning/phases/02-core-data-model/02-RESEARCH.md
@C:/Users/aaron/OneDrive/Claude Code/.planning/phases/02-core-data-model/02-CONTEXT.md
@C:/Users/aaron/OneDrive/Claude Code/.planning/phases/02-core-data-model/02-PATTERNS.md
@C:/Users/aaron/OneDrive/Claude Code/.planning/phases/02-core-data-model/02-UI-SPEC.md
@C:/Users/aaron/OneDrive/Claude Code/.planning/phases/02-core-data-model/02-01-SUMMARY.md

<interfaces>
<!-- Read before implementing — exact patterns to replicate -->

From canary-propos/src/app/(manager)/people/page.tsx (the file being modified):
- Lines 43: isManager check uses === (MUST be updated to .includes() per 0012 migration)
- Lines 94, 140: role display uses {person.role} (MUST be updated to {person.role?.join(', ')})
- Overall RSC structure: session check → callerPerson fetch → data fetches → JSX with table + mobile cards

From canary-propos/src/components/people/InviteUserForm.tsx (form pattern analog):
- 'use client' + useState + useTransition pattern
- Dialog + DialogContent + DialogHeader + DialogTitle + DialogTrigger
- handleSubmit: e.preventDefault() → startTransition(async () => result = await action() → toast.success / setError)
- Form input classes: "w-full rounded-md border border-stone-300 px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-amber-600"
- Submit button: "mt-2 flex min-h-11 w-full items-center justify-center rounded-md bg-amber-600 px-4 py-2 text-sm font-semibold text-white hover:bg-amber-700 disabled:opacity-60"
- Error display: <p className="text-sm text-red-600" role="alert">{error}</p>

From canary-propos/src/app/(manager)/people/actions.ts (server action pattern):
- 'use server' header
- ActionResult = { success: true } | { success: false; error: string }
- getCallerContext() helper: createClient() → auth.getUser() → people row fetch
- Role check: ctx.person.role?.includes('manager') (after 0012 — use includes not ===)
- .insert({ ...parsed.data, org_id: ctx.person.org_id })
- revalidatePath('/people') before return { success: true }

Tabs import (shadcn):
- import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs'
- Verify canary-propos/src/components/ui/tabs.tsx exists; if not, run: npx shadcn add tabs (from canary-propos/ dir)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create contacts server actions and update people/page.tsx with tabs + role[] fixes</name>
  <read_first>
    canary-propos/src/app/(manager)/people/page.tsx
    canary-propos/src/app/(manager)/people/actions.ts
    canary-propos/src/components/people/InviteUserForm.tsx
    canary-propos/src/components/ui/tabs.tsx (check if it exists; if missing, note it)
    .planning/phases/02-core-data-model/02-PATTERNS.md (§Server Actions pattern)
    .planning/phases/02-core-data-model/02-UI-SPEC.md (§1. /people layout)
  </read_first>
  <action>
    STEP A — Check tabs component. If canary-propos/src/components/ui/tabs.tsx does not exist, run from canary-propos/ directory: npx shadcn add tabs

    STEP B — Create canary-propos/src/app/actions/contacts.ts with 'use server' header. Define ActionResult type (copy from people/actions.ts). Define getCallerContext() helper (copy from people/actions.ts exactly — same pattern). Implement three exports:

    createContact(formData: { first_name: string; last_name: string; email: string; phone?: string; roles: string[]; notes?: string }): ActionResult — Zod schema validates: first_name min(1), last_name min(1), email email(), phone optional string, roles array min(1) where each element is in ['tenant', 'owner', 'vendor'] (no 'admin' allowed — security gate), notes optional string. Check ctx.person.role?.includes('manager') || ctx.person.role?.includes('admin'). Insert into people table: { org_id, first_name, last_name, email, phone, role: parsed.roles, notes, active: true }. The role column is text[] so pass the array directly. revalidatePath('/people').

    updateContact(id: string, formData: same shape): ActionResult — Fetch contact by id + org_id to verify ownership. Update with new values. revalidatePath('/people').

    deactivateContact(id: string): ActionResult — Verify ownership. Update active=false. revalidatePath('/people').

    STEP C — Update canary-propos/src/app/(manager)/people/page.tsx:
    1. Add tabs import: import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs'
    2. Add ContactsTab import: import { ContactsTab } from '@/components/people/ContactsTab'
    3. Fix line 43 isManager: change role === 'manager' || role === 'admin' to role?.includes('manager') || role?.includes('admin')
    4. Fix any other role === comparisons in the file (lines ~141)
    5. Add contacts query after existing people query: fetch people with .overlaps('role', ['tenant', 'owner', 'vendor']).eq('active', true)
    6. Update page header: change subtitle to "Manage your team and contacts." and the header CTA to show AddContactForm conditionally when contacts tab is active (or always show both InviteUserForm and AddContactForm buttons)
    7. Wrap existing people list in Tabs: Tabs defaultValue="team", TabsList with Team and Contacts triggers, TabsContent value="team" wraps existing table/cards JSX, TabsContent value="contacts" renders ContactsTab with contacts and isManager props
    8. Fix role display in table (line ~94): change {person.role} to {person.role?.join(', ')} — for team tab the role is still a text[] so join
    9. Fix mobile card role display (~line 140): same join fix
  </action>
  <verify>
    <automated>cd canary-propos && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - contacts.ts exports createContact, updateContact, deactivateContact, ActionResult
    - createContact Zod schema rejects 'admin' in roles array
    - people/page.tsx isManager checks use .includes() not ===
    - people/page.tsx role display uses join(', ')
    - people/page.tsx imports Tabs components and ContactsTab
    - people/page.tsx fetches contacts with .overlaps('role', ['tenant', 'owner', 'vendor'])
    - tsc --noEmit exits 0 for these files
  </acceptance_criteria>
  <done>contacts.ts server actions written; people/page.tsx updated with tab structure and role[] fixes</done>
</task>

<task type="auto">
  <name>Task 2: Create ContactsTab, AddContactForm, and EditContactForm components</name>
  <read_first>
    canary-propos/src/components/people/InviteUserForm.tsx
    canary-propos/src/components/people/RemoveUserDialog.tsx
    canary-propos/src/app/(manager)/people/page.tsx (read the current state after Task 1)
    .planning/phases/02-core-data-model/02-UI-SPEC.md (§1. /people layout — contacts tab spec)
    .planning/phases/02-core-data-model/02-PATTERNS.md (§Client Component pattern)
  </read_first>
  <action>
    FILE: canary-propos/src/components/people/ContactsTab.tsx
    'use client' component. Props interface: { contacts: Array<{ id: string; first_name: string | null; last_name: string | null; email: string; phone: string | null; role: string[]; active: boolean }>; isManager: boolean; orgId: string }. Render:
    - If contacts empty: dashed border empty state (rounded-xl border border-dashed border-stone-200 px-8 py-16 text-center) with heading "No contacts yet", body "Add tenants, owners, and vendors to build your contact directory.", and AddContactForm button if isManager
    - Desktop table (hidden md:block): columns Name, Email, Phone, Role(s), Actions. Role(s) column: map role array and render each as Badge with inline colors per UI-SPEC (tenant: no special color — stone; owner: blue-100/blue-700; vendor: purple-100/purple-700 — Claude's discretion for differentiation). Actions column shows Edit button (opens EditContactForm) and Deactivate button (red text, triggers deactivateContact with confirmation)
    - Mobile cards (md:hidden): name + role badge top row, email, phone, Edit link text-amber-600
    - Use same table Tailwind classes as people/page.tsx desktop table exactly

    FILE: canary-propos/src/components/people/AddContactForm.tsx
    'use client'. Dialog trigger button labeled "Add Contact" using amber-600 style. Form fields per UI-SPEC §1: first_name, last_name, email, phone (optional), roles (checkbox group with Tenant/Owner/Vendor — 3 checkboxes, not a select, required at least one checked), notes textarea 3 rows optional. On submit: startTransition calling createContact(). On success: toast.success('Contact created'), close dialog. On error: show inline error. Props: none (orgId is derived server-side in the action).

    FILE: canary-propos/src/components/people/EditContactForm.tsx
    'use client'. Trigger: "Edit" text button (text-sm text-stone-600 hover:text-stone-900). Opens Dialog pre-filled with contact data. Same fields as AddContactForm. On submit: startTransition calling updateContact(contact.id, formData). Also includes a "Deactivate" button at bottom (text-sm text-red-600) which after a window.confirm() or inline confirmation calls deactivateContact(contact.id). Props: contact (the contact row shape from ContactsTab).
  </action>
  <verify>
    <automated>cd canary-propos && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - ContactsTab.tsx renders desktop table + mobile cards + empty state, accepts contacts/isManager/orgId props
    - AddContactForm.tsx has checkbox inputs for roles (not a select), validates at least one role selected
    - EditContactForm.tsx pre-fills form fields from contact prop, calls updateContact on submit
    - No 'admin' option appears in AddContactForm or EditContactForm role checkboxes
    - tsc --noEmit exits 0
    - Visiting /people in browser shows Team and Contacts tabs (human verify in plan 02-06 checkpoint)
  </acceptance_criteria>
  <done>ContactsTab, AddContactForm, EditContactForm components created and TypeScript-clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AddContactForm → createContact | Form submits roles array; server action must validate no 'admin' value reaches DB |
| Manager → deactivateContact | Only managers can deactivate; action verifies org membership and role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Elevation of Privilege | createContact roles field | mitigate | Zod schema CHECK: z.array(z.enum(['tenant','owner','vendor'])) — 'admin' literal is not in the enum; will fail validation |
| T-02-07 | Tampering | updateContact without org ownership | mitigate | getCallerContext() + explicit .eq('org_id', ctx.person.org_id) in fetch-before-update |
| T-02-08 | Information Disclosure | contacts query in RSC | mitigate | .eq('org_id', callerPerson.org_id) scopes query; RLS also enforces org boundary |
| T-02-SC | Tampering | npm installs | accept | Only "npx shadcn add tabs" if tabs component missing — official shadcn CLI, not a third-party package |
</threat_model>

<verification>
Manual verification (no automated test framework configured):
1. Visit /people — two tabs visible: Team and Contacts
2. Contacts tab shows empty state with Add Contact button
3. Click Add Contact → Dialog opens with first_name, last_name, email, phone, role checkboxes (Tenant/Owner/Vendor), notes
4. Submit with email only → validation error shown
5. Submit valid contact → row appears in Contacts tab, toast "Contact created"
6. Click Edit → dialog pre-filled → change phone → save → toast "Changes saved"
7. Click Deactivate → confirmation → contact removed from list
8. Team tab still shows original people list unchanged
</verification>

<success_criteria>
- /people renders two tabs: Team (existing list) and Contacts (new)
- Adding a contact with roles ['tenant', 'owner'] creates a row with role text[] = {'tenant','owner'}
- role text[] displays as "tenant, owner" (joined)
- deactivateContact sets active=false; contact disappears from list
- No TypeScript errors (tsc --noEmit passes)
- isManager check correctly uses .includes() — existing Team tab Remove buttons still work
</success_criteria>

<output>
Create .planning/phases/02-core-data-model/02-02-SUMMARY.md when done
</output>
