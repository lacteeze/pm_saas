---
phase: 02-core-data-model
plan: 05
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-03"
  - "02-04"
files_modified:
  - canary-propos/src/app/(manager)/dashboard/page.tsx
  - canary-propos/src/components/dashboard/SummaryCards.tsx
autonomous: true
requirements:
  - PROP-05
  - LEASE-03

must_haves:
  truths:
    - "Dashboard shows 4 summary cards: Total Units, Occupied, Vacant, Active Leases"
    - "Summary cards use actual counts from the database (not hardcoded values)"
    - "Dashboard shows lease expiry alert section below summary cards if any leases expire within 90 days"
    - "Expiry alerts grouped into 3 urgency buckets (≤30 red, 31–60 amber, 61–90 stone)"
    - "SetupBanner (existing) remains above the summary cards"
    - "Dashboard uses 4-card grid on desktop, 2×2 on mobile"
  artifacts:
    - path: "canary-propos/src/app/(manager)/dashboard/page.tsx"
      provides: "Updated dashboard RSC with summary metrics and expiry alerts"
    - path: "canary-propos/src/components/dashboard/SummaryCards.tsx"
      provides: "4-card grid component"
      exports: ["SummaryCards"]
  key_links:
    - from: "canary-propos/src/app/(manager)/dashboard/page.tsx"
      to: "public.units + public.leases"
      via: "Promise.all aggregate counts using supabase count queries"
      pattern: "Promise.all.*count.*units.*leases"
    - from: "canary-propos/src/app/(manager)/dashboard/page.tsx"
      to: "canary-propos/src/components/leases/ExpiryAlertCallout.tsx"
      via: "expiring leases passed as prop"
      pattern: "ExpiryAlertCallout"
---

<objective>
Replace the dashboard placeholder with real summary metrics (4 count cards) and a lease expiry alert section. SummaryCards is a new server-side-rendered component; ExpiryAlertCallout (created in Plan 02-03) is reused.

Purpose: Satisfies PROP-05 and LEASE-03 on the manager dashboard. Gives managers an at-a-glance view of portfolio health as the first thing they see on login.

Output: Dashboard with live unit counts, active lease count, and expiry alerts at 90/60/30 days.
</objective>

<execution_context>
@C:/Users/aaron/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/aaron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/aaron/OneDrive/Claude Code/.planning/phases/02-core-data-model/02-RESEARCH.md
@C:/Users/aaron/OneDrive/Claude Code/.planning/phases/02-core-data-model/02-UI-SPEC.md
@C:/Users/aaron/OneDrive/Claude Code/.planning/phases/02-core-data-model/02-PATTERNS.md
@C:/Users/aaron/OneDrive/Claude Code/.planning/phases/02-core-data-model/02-01-SUMMARY.md
@C:/Users/aaron/OneDrive/Claude Code/.planning/phases/02-core-data-model/02-04-SUMMARY.md

<interfaces>
<!-- Read current dashboard file before modifying -->

From canary-propos/src/app/(manager)/dashboard/page.tsx (current file — read it before writing):
- Session + callerPerson pattern already in place
- SetupBanner already imported and rendered — retain this exactly

Dashboard aggregate query pattern (from RESEARCH.md §Dashboard Data Fetching):
  const orgId = callerPerson.org_id
  const [
    { count: totalUnits },
    { count: occupiedUnits },
    { count: vacantUnits },
    { count: activeLeases },
  ] = await Promise.all([
    supabase.from('units').select('*', { count: 'exact', head: true }).eq('org_id', orgId),
    supabase.from('units').select('*', { count: 'exact', head: true }).eq('org_id', orgId).eq('status', 'occupied'),
    supabase.from('units').select('*', { count: 'exact', head: true }).eq('org_id', orgId).eq('status', 'vacant'),
    supabase.from('leases').select('*', { count: 'exact', head: true }).eq('org_id', orgId).eq('status', 'active'),
  ])

Expiry query (90-day window — same pattern as leases/page.tsx):
  const ninetyDaysFromNow = new Date(); ninetyDaysFromNow.setDate(ninetyDaysFromNow.getDate() + 90)
  fetch leases with end_date <= ninetyDaysFromNow AND status='active' with people!tenant_id + units!unit_id + properties!property_id join
  compute daysUntilExpiry for each lease

SummaryCards props:
  totalUnits: number, occupiedUnits: number, vacantUnits: number, activeLeases: number

Icons (from Lucide React, already installed):
  Building2 (Total Units), Users (Occupied), DoorOpen (Vacant), FileText (Active Leases)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SummaryCards component and update dashboard/page.tsx</name>
  <read_first>
    canary-propos/src/app/(manager)/dashboard/page.tsx
    canary-propos/src/components/onboarding/SetupBanner.tsx (to understand the existing dashboard pattern)
    canary-propos/src/components/leases/ExpiryAlertCallout.tsx (written in Plan 02-03 — confirm props interface)
    .planning/phases/02-core-data-model/02-UI-SPEC.md (§7. /dashboard layout)
    .planning/phases/02-core-data-model/02-PATTERNS.md (§Dashboard RSC update pattern)
  </read_first>
  <action>
    FILE: canary-propos/src/components/dashboard/SummaryCards.tsx
    This can be a simple server component (no interactivity needed) or a plain functional component (not 'use client' — receives props from parent RSC). Props: { totalUnits: number; occupiedUnits: number; vacantUnits: number; activeLeases: number }. Render a 4-column grid on desktop and 2×2 on mobile: className="grid grid-cols-2 gap-3 md:grid-cols-4 md:gap-4". Each card: rounded-xl border border-stone-200 bg-white p-4. Inside each card: icon (20px stone-400) on top, then the metric value (text-xl font-semibold text-stone-900), then label (text-xs text-stone-500 uppercase tracking-wide). Four cards per UI-SPEC:
    - Building2 icon, totalUnits value, "Total Units" label
    - Users icon, occupiedUnits value, "Occupied" label
    - DoorOpen icon, vacantUnits value, "Vacant" label
    - FileText icon, activeLeases value, "Active Leases" label

    FILE: canary-propos/src/app/(manager)/dashboard/page.tsx (UPDATE — read first, then extend)
    1. Retain existing session check, callerPerson fetch, org fetch, and SetupBanner render — do NOT remove these
    2. Add imports: SummaryCards, ExpiryAlertCallout, and Lucide icons used in SummaryCards (if needed, though SummaryCards encapsulates them)
    3. After the existing callerPerson/org fetches, add parallel aggregate queries using Promise.all (see interfaces above for exact pattern)
    4. Fetch expiring leases: query leases where end_date within 90 days and status='active', include people!tenant_id(first_name, last_name) and units!unit_id(unit_number, properties!property_id(street_address, city)). Map results to compute daysUntilExpiry (Math.ceil days diff from today). Pass to ExpiryAlertCallout as the leases prop with shape: { id, tenantName: first+last, propertyUnit: `${city} — Unit ${unit_number}`, endDate, daysUntilExpiry }
    5. In JSX, add after SetupBanner: h1 "Dashboard", SummaryCards with the 4 counts, then conditional ExpiryAlertCallout (only if expiringLeases.length > 0)
    6. Retain any existing JSX from the current dashboard below the new content (or replace the placeholder text if it exists)
  </action>
  <verify>
    <automated>cd canary-propos && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - SummaryCards.tsx renders 4 cards in a responsive grid (2-col mobile, 4-col desktop)
    - SummaryCards has no 'use client' directive (it receives static props from RSC)
    - dashboard/page.tsx uses Promise.all for all 4 count queries
    - SetupBanner is still rendered above the new dashboard content
    - ExpiryAlertCallout is imported from correct path (src/components/leases/ExpiryAlertCallout.tsx)
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>SummaryCards component created; dashboard updated with live metrics and expiry alerts</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard RSC → Supabase | All queries scoped by org_id from callerPerson row; RLS also enforces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-17 | Information Disclosure | Dashboard showing counts from wrong org | mitigate | All queries use .eq('org_id', callerPerson.org_id); org_id comes from authenticated people row, not JWT |
| T-02-18 | Information Disclosure | Expiry alert showing another org's leases | mitigate | Lease query scoped with same callerPerson.org_id; RLS also filters |
| T-02-SC | Tampering | npm installs | accept | No new packages; all Lucide icons already installed |
</threat_model>

<verification>
Manual verification:
1. Sign in as manager, visit /dashboard
2. Summary cards row appears with numeric values (0s if no data yet, real counts if data exists)
3. Create some units and a lease with end_date 25 days from today
4. Refresh dashboard — Total Units count matches, Active Leases = 1
5. Lease expiry section appears below summary cards with red callout for the 25-day lease
6. SetupBanner still shows above the dashboard content (if setup is incomplete)
7. Mobile: cards display in 2×2 grid (use browser dev tools to simulate mobile)
</verification>

<success_criteria>
- Dashboard shows 4 summary cards with live database counts
- Total Units, Occupied, Vacant counts match actual unit records
- Active Leases count matches leases with status='active'
- Expiry alerts section renders for leases within 90 days, grouped by urgency
- SetupBanner retained and renders correctly
- No TypeScript errors
</success_criteria>

<output>
Create .planning/phases/02-core-data-model/02-05-SUMMARY.md when done
</output>
