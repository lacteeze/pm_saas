---
phase: 05-maintenance
plan: 04
type: execute
wave: 3
depends_on:
  - 05-02
  - 05-03
files_modified:
  - src/lib/work-orders/sms.ts
  - src/components/work-orders/AssignVendorDialog.tsx
  - src/app/(vendor-nologin)/vendor/jobs/[token]/page.tsx
autonomous: true
requirements:
  - MAINT-03
  - MAINT-04
  - MAINT-05

must_haves:
  truths:
    - "Manager can assign a work order to a vendor via a dialog that collects vendor selection and estimated_cost"
    - "Non-portal vendor receives Pingram SMS + Resend email with the no-login link on assignment"
    - "No-login vendor page at /vendor/jobs/[token] renders job details without requiring auth"
    - "Vendor can mark in_progress and completed from the no-login page"
    - "Vendor can submit an invoice dollar amount from the no-login page"
    - "Closed or invalid token shows a 404 / friendly 'link expired' page"
  artifacts:
    - path: "src/lib/work-orders/sms.ts"
      provides: "sendVendorJobSMS helper wrapping pingram SDK"
      exports: ["sendVendorJobSMS"]
    - path: "src/components/work-orders/AssignVendorDialog.tsx"
      provides: "Dialog for assigning vendor + entering estimated_cost"
      contains: "updateWorkOrderStatus"
    - path: "src/app/(vendor-nologin)/vendor/jobs/[token]/page.tsx"
      provides: "No-login RSC page validating vendor_token, rendering job info + action form"
      contains: "createAdminClient"
  key_links:
    - from: "src/lib/work-orders/sms.ts"
      to: "pingram SDK"
      via: "new Pingram({ apiKey: PINGRAM_API_KEY })"
      pattern: "pingram\\.send"
    - from: "src/app/(vendor-nologin)/vendor/jobs/[token]/page.tsx"
      to: "src/lib/supabase/admin.ts"
      via: "createAdminClient()"
      pattern: "createAdminClient"
---

<objective>
Build vendor assignment (manager side) and the no-login vendor job page, wiring Pingram SMS and Resend email notifications on assignment.

Purpose: Completes the vendor notification loop — manager assigns, vendor receives SMS+email, vendor updates status via no-login link.
Output: sms.ts (Pingram wrapper), AssignVendorDialog, vendor no-login page + server action.
</objective>

<execution_context>
@C:\Users\aaron\OneDrive\Claude Code\.planning\phases\05-maintenance\05-CONTEXT.md
@C:\Users\aaron\OneDrive\Claude Code\.planning\phases\05-maintenance\05-RESEARCH.md
</execution_context>

<context>
@C:\Users\aaron\OneDrive\Claude Code\.planning\ROADMAP.md
@C:\Users\aaron\OneDrive\Claude Code\canary-propos\src\app\(public)\listings\[id]\page.tsx
@C:\Users\aaron\OneDrive\Claude Code\canary-propos\src\lib\supabase\admin.ts

<interfaces>
<!-- updateWorkOrderStatus from 05-02 — called by AssignVendorDialog -->
import { updateWorkOrderStatus } from '@/app/actions/work-orders'
// For assignment: updateWorkOrderStatus(workOrderId, 'assigned', {
//   assigned_vendor_id: string,
//   estimated_cost: number
// })
// If estimated_cost > 500, action internally routes to pending_approval (transparent to dialog)

<!-- updateViaVendorToken from 05-02 — called by vendor no-login page form -->
import { updateViaVendorToken } from '@/app/actions/work-orders'
// updateViaVendorToken(vendorToken, 'in_progress' | 'completed', invoiceAmount?)

<!-- createAdminClient from src/lib/supabase/admin.ts — use in vendor no-login RSC -->
// DO NOT use createPublicClient() — work_orders has no anon read policy
// The vendor_token is the credential; validate it server-side with admin client

<!-- Pingram SDK (pingram v1.0.14) -->
import Pingram from 'pingram'
const client = new Pingram({ apiKey: process.env.PINGRAM_API_KEY! })
// Send call (ASSUMED field names from RESEARCH.md Pattern 3 — verify against node_modules/pingram):
client.send({
  type: 'vendor_job_assignment',   // must exist in Pingram dashboard
  to: { id: vendorPhone, number: vendorPhone },
  sms: { message: string }
})
// If actual SDK API differs from above, adapt to match the real pingram package interface
// found in node_modules/pingram/dist/index.d.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pingram SMS wrapper + AssignVendorDialog</name>
  <files>
    src/lib/work-orders/sms.ts,
    src/components/work-orders/AssignVendorDialog.tsx
  </files>
  <action>
    **FIRST: Read `canary-propos/node_modules/pingram/dist/index.d.ts`** (or equivalent type declaration file) to confirm the exact Pingram SDK constructor and `.send()` method signatures before writing sms.ts. Do not rely solely on RESEARCH.md Pattern 3 field names — verify against the actual installed package. If the field names differ from `{ type, to: { id, number }, sms: { message } }`, use the real API.

    Create `src/lib/work-orders/sms.ts`:
    - Import Pingram SDK using the confirmed signature
    - Export `sendVendorJobSMS(params: { vendorPhone: string, propertyAddress: string, jobDescription: string, noLoginLink: string }): Promise<void>`
    - Phone must be in E.164 format (+16135551234). Add a check: if phone doesn't start with '+', prepend '+1' as a Canadian default (for numbers stored without prefix)
    - Message body (per D-11): "New job from Canary Property Management:\nProperty: {address}\nJob: {description}\nView + update status: {link}\nQuestions? Call us at [org phone from env or hardcoded placeholder]"
    - Use notification type `'vendor_job_assignment'` (must exist in Pingram dashboard — see RESEARCH.md Pitfall 1)
    - Wrap the `pingram.send()` call in try/catch — SMS failure must not block work order assignment (per RESEARCH.md Pitfall 5). Log the error with `console.error('[sms:vendor_job_assignment]', err)` but do not throw.
    - For Canadian region, check if Pingram constructor accepts a `baseURL` option; if so, use `'https://api.ca.pingram.io'`.

    Create `src/components/work-orders/AssignVendorDialog.tsx` (client component):
    - Triggered from the work order detail page (`src/app/(manager)/maintenance/[id]/page.tsx` in Plan 05-03). Add an "Assign Vendor" button to that page that opens this dialog — this requires a minor edit to the 05-03 detail page.
    - Dialog form fields:
      1. Vendor select: dropdown of people with role includes 'vendor' (fetched server-side and passed as prop)
      2. Estimated cost: number input (required, min 0)
      3. Note field (optional): free text note for the vendor
    - On submit: calls `updateWorkOrderStatus(workOrderId, 'assigned', { assigned_vendor_id, estimated_cost })`
    - After success: if result was actually routed to pending_approval (server returned a flag or estimated_cost > 500 is known), show a toast "Work order sent to owner for approval (cost > $500)". Otherwise show "Vendor assigned and notified."
    - The SMS + email notification is fired inside `updateWorkOrderStatus` → `sendVendorAssignmentNotifications` (to be wired in notifications.ts below).
    - Update `src/lib/work-orders/notifications.ts` (created in 05-02) to also export `sendVendorAssignmentNotifications(wo, workOrderId)` which:
      1. Fetches vendor phone and email from people table (admin client)
      2. Fetches property address for the work order
      3. If vendor.phone exists: calls `sendVendorJobSMS(...)` (non-blocking, fire-and-forget)
      4. Always: sends Resend email to vendor with job details + no-login link (per D-12)
      5. No-login link = `https://app.canarypm.ca/vendor/jobs/${wo.vendor_token}`
    - Wire `sendVendorAssignmentNotifications` call into `updateWorkOrderStatus` in `src/app/actions/work-orders.ts` (05-02 file): when actualNewStatus === 'assigned', call it.
  </action>
  <verify>
    <automated>cd canary-propos && npx tsc --noEmit 2>&1 | grep "sms\|AssignVendor" | head -10</automated>
  </verify>
  <done>sms.ts wraps Pingram with try/catch. AssignVendorDialog opens from detail page, collects vendor + cost, calls updateWorkOrderStatus. sendVendorAssignmentNotifications fires SMS + email on assignment. No TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: No-login vendor job page</name>
  <files>
    src/app/(vendor-nologin)/vendor/jobs/[token]/page.tsx
  </files>
  <action>
    Create the route group directory `src/app/(vendor-nologin)/vendor/jobs/[token]/`. This route group has no layout.tsx — it renders completely standalone (no app nav, no auth). The middleware already passes `/vendor/jobs/*` through (from Plan 05-01).

    Create `src/app/(vendor-nologin)/vendor/jobs/[token]/page.tsx` as an async RSC:

    ```
    export default async function VendorJobPage({ params }: { params: Promise<{ token: string }> })
    ```

    Read `canary-propos/node_modules/next/dist/docs/` or check the existing `(public)/listings/[id]/page.tsx` for the correct Next.js 15 params handling pattern (params is a Promise in Next.js 15 App Router).

    Page logic:
    1. Await params to get token
    2. Use `createAdminClient()` (NOT createPublicClient, NOT createClient — anon cannot read work_orders) to query:
       ```
       work_orders where vendor_token = token AND status != 'closed'
       with joins: properties(street_address, city), units(unit_number)
       select: id, title, description, priority, status, vendor_token,
               estimated_cost, vendor_cost, billed_amount, created_at
       ```
       Do NOT select owner_approve_token or owner_decline_token.
    3. If no row found (invalid token or closed work order): render a minimal page "This link is no longer active. The work order may have been closed." — do NOT use `notFound()` as that leaks 404 meaning; use a custom message page instead.
    4. If found: render a clean, mobile-friendly page with:
       - Header: "Canary Property Management — Work Order"
       - Job info: property address + unit (if any), title, description, priority badge
       - Current status displayed prominently
       - Action section based on current status:
         - If status = 'assigned': show "Start Work" button → calls updateViaVendorToken(token, 'in_progress')
         - If status = 'in_progress': show "Mark Complete" form with optional invoice amount (number input, $0 = no invoice) → calls updateViaVendorToken(token, 'completed', invoiceAmount)
         - If status = 'completed' or 'approved' or 'pending_approval': show "Work order submitted — thank you. Canary Property Management has been notified."
         - If status = 'draft' or 'submitted': show "This work order has not been assigned yet. Please contact Canary Property Management."
       - The action buttons are client components (or use a small inline client form component within this RSC page)
       - No login prompt, no navigation bar, no links to other parts of the app

    The page must be fully functional with just HTML/CSS (Tailwind) — no session dependency.

    Call `updateViaVendorToken` (from 05-02) via a server action form or a small inline client component. Use `useTransition` or a simple form `action=` attribute pointing to the server action.
  </action>
  <verify>
    <automated>cd canary-propos && npx tsc --noEmit 2>&1 | grep "vendor" | head -10</automated>
  </verify>
  <done>No-login vendor page renders for a valid token without auth. Invalid/closed token shows friendly message. Status-appropriate action buttons appear. No token columns exposed. No TypeScript errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public internet → /vendor/jobs/[token] | No auth; token is the only credential — validated server-side via admin client |
| Pingram API call → SMS delivery | API key server-only; failure is non-blocking |
| AssignVendorDialog → updateWorkOrderStatus | Authenticated manager session; server action re-validates role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Elevation of Privilege | Vendor no-login page — admin client | mitigate | Admin client used ONLY for the token lookup; no write capability with admin client — writes go through updateViaVendorToken which re-validates token |
| T-05-13 | Information Disclosure | Vendor no-login page — cost columns | mitigate | billed_amount deliberately NOT selected in vendor query — markup not visible to vendor |
| T-05-14 | Spoofing | SMS from Canary domain | accept | Pingram handles sender ID; carrier-level spoofing is outside app scope |
| T-05-15 | Information Disclosure | PINGRAM_API_KEY server exposure | mitigate | Server-only env var; never NEXT_PUBLIC_; sms.ts is server-side only |
| T-05-SC | Tampering | pingram npm package | mitigate | Approved in 05-01 Package Audit; blocking human checkpoint in 05-01 before SMS code activates |
</threat_model>

<verification>
- sms.ts uses confirmed Pingram SDK API (verified from node_modules type declarations)
- AssignVendorDialog opens from the work order detail page
- Vendor receives SMS (if phone exists) and email on assignment
- /vendor/jobs/[token] page loads without auth session
- Invalid/closed token shows friendly message not a raw 404
- Action buttons match the current work order status
- billed_amount NOT exposed on vendor page
- `npx tsc --noEmit` clean for these files
</verification>

<success_criteria>
Non-portal vendors can be assigned, receive SMS+email notifications, and update job status via the no-login link. The vendor page is a standalone, session-free mobile-friendly page.
</success_criteria>

<output>
Create `.planning/phases/05-maintenance/05-04-SUMMARY.md` when done.
</output>
