---
phase: 05-maintenance
plan: 06
type: execute
wave: 4
depends_on:
  - 05-04
  - 05-05
files_modified:
  - src/app/(tenant)/my-home/maintenance/page.tsx
  - src/app/(tenant)/my-home/maintenance/new/page.tsx
autonomous: true
requirements:
  - MAINT-01
  - MAINT-02
  - MAINT-08
  - MAINT-09

must_haves:
  truths:
    - "Tenant can submit a maintenance request from the tenant portal"
    - "Tenant can view a list of their own work orders with status"
    - "Tenant work order queries never expose vendor_cost, billed_amount, or any token columns"
    - "Expense auto-creation on completion is verified end-to-end"
  artifacts:
    - path: "src/app/(tenant)/my-home/maintenance/page.tsx"
      provides: "Tenant work order list (RSC, scoped to tenant's own work orders)"
      contains: "my-home"
    - path: "src/app/(tenant)/my-home/maintenance/new/page.tsx"
      provides: "Tenant create work order form (limited fields vs manager form)"
      contains: "createWorkOrder"
  key_links:
    - from: "src/app/(tenant)/my-home/maintenance/page.tsx"
      to: "work_orders table"
      via: "createClient() with RLS tenants_select_own policy"
      pattern: "work_orders.*created_by"
    - from: "src/app/(tenant)/my-home/maintenance/new/page.tsx"
      to: "src/app/actions/work-orders.ts"
      via: "createWorkOrder server action"
      pattern: "createWorkOrder"
---

<objective>
Add the tenant-facing maintenance surfaces to the tenant portal and verify the expense auto-creation end-to-end.

Purpose: Completes MAINT-01 (tenant can create a work order) and provides the tenant with visibility into their requests. This is the final Wave 4 plan — all server-side logic (05-02 through 05-05) is already built.
Output: Tenant work order list page, tenant create form. No new server actions — reuses createWorkOrder from 05-02.
</objective>

<execution_context>
@C:\Users\aaron\OneDrive\Claude Code\.planning\phases\05-maintenance\05-CONTEXT.md
@C:\Users\aaron\OneDrive\Claude Code\.planning\phases\05-maintenance\05-RESEARCH.md
</execution_context>

<context>
@C:\Users\aaron\OneDrive\Claude Code\.planning\ROADMAP.md
@C:\Users\aaron\OneDrive\Claude Code\canary-propos\src\app\(tenant)\my-home\page.tsx

<interfaces>
<!-- createWorkOrder from 05-02 — tenant calls this directly -->
import { createWorkOrder } from '@/app/actions/work-orders'
// createWorkOrder({ property_id, unit_id?, title, description, priority })
// Server action validates role includes 'tenant' and scopes to caller's org

<!-- RLS tenants_select_own policy (from 05-01 migration) -->
// Tenants can SELECT work_orders where created_by = their people.id
// This is enforced by the DB — no extra filter needed in the RSC query
// BUT: never SELECT * — explicitly list safe columns only

<!-- Safe columns for tenant queries (NEVER include these): -->
// EXCLUDE: vendor_cost, billed_amount, estimated_cost, vendor_token,
//          owner_approve_token, owner_decline_token, assigned_vendor_id

<!-- Tenant work order safe columns to SELECT: -->
// id, title, description, priority, status, created_at, updated_at
// + properties(street_address, city) join for context
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tenant work order list + create form</name>
  <files>
    src/app/(tenant)/my-home/maintenance/page.tsx,
    src/app/(tenant)/my-home/maintenance/new/page.tsx
  </files>
  <action>
    Read `src/app/(tenant)/my-home/page.tsx` to understand the tenant portal layout, navigation, and existing component patterns before creating new pages.

    **`src/app/(tenant)/my-home/maintenance/page.tsx`** (RSC):
    - Fetch work orders using `createClient()` (session-based). The RLS `tenants_select_own` policy automatically scopes to the caller's created_by. Select safe columns ONLY:
      ```
      id, title, description, priority, status, created_at, updated_at,
      properties(street_address, city)
      ```
      NEVER select: vendor_cost, billed_amount, estimated_cost, vendor_token, owner_approve_token, owner_decline_token, assigned_vendor_id.
    - Render: page title "My Maintenance Requests" with a "Submit Request" button linking to `/my-home/maintenance/new`.
    - Desktop: simple table — Title, Property, Priority, Status (WorkOrderStatusBadge), Submitted date.
    - Mobile: card list.
    - Empty state: "No maintenance requests yet." with a "Submit your first request" link.
    - Add a link to `/my-home/maintenance` in the tenant portal navigation (edit the my-home layout or nav component as needed to include "Maintenance" nav item).

    **`src/app/(tenant)/my-home/maintenance/new/page.tsx`** (client component with react-hook-form):
    - Tenant form is simpler than the manager form — no assigned_vendor_id, no cost fields, no unit select (tenant's unit is known from their lease).
    - Fields: title (text, required), description (textarea, required), priority (select: low/medium/high/urgent, default medium).
    - The property_id and unit_id are resolved server-side from the tenant's active lease — pass them as hidden values or resolve in the server action. Fetch the tenant's active lease in the RSC wrapper to get property_id and unit_id, pass to the client form.
    - On submit: calls `createWorkOrder({ property_id, unit_id, title, description, priority })`.
    - On success: redirect to `/my-home/maintenance` with a toast "Your request has been submitted."
    - Zod validation: title min 3 chars, description min 10 chars.

    Also add a "Maintenance" card or link to the tenant dashboard at `/my-home` showing open work order count. Edit `src/app/(tenant)/my-home/page.tsx` to include this summary if there is a dashboard-style layout.
  </action>
  <verify>
    <automated>cd canary-propos && npx tsc --noEmit 2>&1 | grep "my-home/maintenance" | head -10</automated>
  </verify>
  <done>Tenant can view their work orders at /my-home/maintenance. Tenant can submit a new request via /my-home/maintenance/new. No cost or token columns in tenant queries. TypeScript clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 5 maintenance feature. All six plans delivered:
    - Schema + middleware (05-01)
    - State machine + server actions (05-02)
    - Manager work order UI (05-03)
    - Vendor assignment + Pingram SMS + no-login vendor page (05-04)
    - Owner approval email + approve/decline no-login pages (05-05)
    - Tenant portal maintenance surfaces (this plan)
  </what-built>
  <how-to-verify>
    **Work order lifecycle test (manager flow):**
    1. Visit `/maintenance/new` — create a work order with priority "high". Confirm it appears in `/maintenance` with status "Draft".
    2. Open the work order detail. Click "Submit Work Order". Status → "Submitted".
    3. Click "Assign Vendor". Select a vendor. Enter estimated cost $200. Confirm status → "Assigned". Check that the assigned vendor's email inbox received a job notification email.
    4. Assign a second work order with estimated cost $750. Confirm it routes to "Pending Approval" (not "Assigned"). Check that the property owner's email inbox received the approval email with Approve + Decline buttons.

    **Owner approval test:**
    5. Click the "Approve Work Order" link in the owner email. Confirm browser shows "Work order approved." Confirm DB status = "Assigned".
    6. Click the "Decline" link in a second test email. Confirm decline form appears. Submit with a note. Confirm DB status = "Closed" with owner_decline_note set.

    **Vendor no-login test:**
    7. Open the no-login vendor link from the assignment email: `/vendor/jobs/[token]`. Confirm page loads without login prompt showing job details.
    8. Click "Start Work". Confirm status → "In Progress".
    9. Click "Mark Complete" with invoice amount $180. Confirm status → "Completed".
    10. Check `expenses` table — confirm a new expense record was created with vendor_cost = 180.

    **Tenant flow test:**
    11. Log in as a tenant. Visit `/my-home/maintenance`. Confirm no vendor_cost or billed_amount visible. Submit a new request. Confirm it appears in the list.

    **Security checks:**
    12. Visit the approval link a second time. Confirm "already used" message (not a DB error).
    13. Try `/vendor/jobs/invalid-uuid`. Confirm friendly "link no longer active" page.
  </how-to-verify>
  <resume-signal>Type "verified" after completing the checks. List any failures with the step number.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tenant session → work_orders SELECT | RLS tenants_select_own limits to created_by = their person ID; no cross-tenant data |
| Tenant form → createWorkOrder | Server action validates role includes 'tenant' and org scoping before insert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-20 | Information Disclosure | Tenant work order query — cost/token columns | mitigate | Explicit column list in RSC query; never SELECT *; reviewed in verify step |
| T-05-21 | Elevation of Privilege | Tenant creating work order for another org | mitigate | createWorkOrder server action resolves org_id from ctx.person (session-bound), not from form input |
| T-05-22 | Information Disclosure | Tenant sees assigned vendor contact info | mitigate | assigned_vendor_id not selected in tenant queries; vendor name/phone not exposed |
</threat_model>

<verification>
- Tenant list page only selects safe columns (no cost, no token, no vendor FK)
- Tenant can submit a work order; it appears in manager's /maintenance list
- Expense record created in expenses table when work order marked completed
- End-to-end approval flow works: assignment → pending_approval → owner email → approve → assigned
- Token one-time use confirmed (second use shows friendly message)
- `npx tsc --noEmit` clean across all Phase 5 files
</verification>

<success_criteria>
All 9 MAINT requirements are implemented. Work orders flow from creation through all 8 states. Vendors receive SMS+email and can update status via no-login link. Owners approve/decline high-cost jobs via email links. Costs auto-create expenses on completion.
</success_criteria>

<output>
Create `.planning/phases/05-maintenance/05-06-SUMMARY.md` when done.
</output>
