---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - tsconfig.json
  - components.json
  - src/app/globals.css
  - src/lib/supabase/server.ts
  - src/lib/supabase/client.ts
  - src/lib/supabase/admin.ts
  - .env.example
  - .env.local
  - vitest.config.ts
  - playwright.config.ts
  - tests/helpers/supabase-test-client.ts
  - scripts/check-rls.ts
autonomous: false
requirements: [FOUND-01, FOUND-13]
user_setup:
  - service: supabase
    why: "Hosted PostgreSQL + Auth + Storage + Realtime — the entire backend"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API -> Project URL"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> anon public key"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> service_role key (NEVER prefix NEXT_PUBLIC_)"
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard -> Account -> Access Tokens (for non-interactive CLI db push)"
      - name: SUPABASE_DB_PASSWORD
        source: "Supabase Dashboard -> Project Settings -> Database -> Connection password"
    dashboard_config:
      - task: "Create Supabase project in ca-central-1 region (PIPEDA data residency — region CANNOT be changed after creation)"
        location: "Supabase Dashboard -> New Project -> Region: Canada (Central)"

must_haves:
  truths:
    - "A developer can run `next dev` and the app boots without errors"
    - "Supabase server, browser, and admin clients exist and import from the correct packages"
    - "The RLS CI linter script exists and can connect to the database"
    - "Vitest runs and the test client fixture can authenticate per role"
  artifacts:
    - path: "src/lib/supabase/server.ts"
      provides: "createServerClient via @supabase/ssr for Server Components/Actions"
      contains: "createServerClient"
    - path: "src/lib/supabase/client.ts"
      provides: "createBrowserClient via @supabase/ssr for Client Components"
      contains: "createBrowserClient"
    - path: "src/lib/supabase/admin.ts"
      provides: "service_role client, server-only"
      contains: "SUPABASE_SERVICE_ROLE_KEY"
    - path: "scripts/check-rls.ts"
      provides: "CI linter that fails build if any public table lacks RLS"
      contains: "rowsecurity"
    - path: "vitest.config.ts"
      provides: "test runner configuration"
    - path: "tests/helpers/supabase-test-client.ts"
      provides: "authenticated test client fixture per role"
  key_links:
    - from: "src/lib/supabase/admin.ts"
      to: "process.env.SUPABASE_SERVICE_ROLE_KEY"
      via: "env var read without NEXT_PUBLIC_ prefix"
      pattern: "SUPABASE_SERVICE_ROLE_KEY"
---

<objective>
Scaffold the Next.js 15 + Supabase project and establish all foundational tooling that every subsequent Phase 1 plan depends on: the three Supabase client factories (`@supabase/ssr`), the test framework (Vitest + Playwright), the RLS CI linter, and shadcn/ui initialization.

Purpose: Getting the Supabase client choice wrong (`@supabase/auth-helpers-nextjs` instead of `@supabase/ssr`) or exposing the service_role key is a rewrite trigger. This plan locks in the correct primitives before any feature code is written.
Output: A booting Next.js app, three Supabase clients, test harness, RLS linter, and shadcn/ui config.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js project and install Phase 1 dependencies</name>
  <files>package.json, next.config.ts, tsconfig.json, components.json, src/app/globals.css, .env.example, .env.local</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Standard Stack section — exact verified versions; Installation section)
    - .planning/phases/01-foundation/01-UI-SPEC.md (Design System + Component Inventory — shadcn init settings and components to add)
    - CLAUDE.md (Technology Stack — confirm @supabase/ssr, no ORM, no Redux)
  </read_first>
  <action>
    Run `npx create-next-app@latest` in the project root with TypeScript, Tailwind, App Router, and `src/` directory enabled. Install runtime deps at the verified versions from RESEARCH.md Standard Stack: `@supabase/supabase-js@2.108.2`, `@supabase/ssr@0.12.0`, `react-hook-form@7.79.0`, `zod@4.4.3`, `@hookform/resolvers@5.4.0`, `resend@6.14.0`, `react-email@6.6.3`, `@react-email/components`, `lucide-react@1.21.0`. Initialize shadcn/ui via `npx shadcn@latest init` with New York style, neutral base color, CSS variables enabled, TypeScript. Add shadcn components: button input label form card separator avatar badge alert dialog dropdown-menu progress select sonner. Do NOT install stripe, twilio, @react-pdf/renderer, pdf-lib, or openai (Phase 4+ deps). Create `.env.example` documenting all required env vars listed in this plan's `user_setup`, and `.env.local` with placeholder values. CRITICAL: `SUPABASE_SERVICE_ROLE_KEY` must NOT have a `NEXT_PUBLIC_` prefix.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "@supabase/ssr" package.json && grep -q "components.json" <(ls) && ! grep -q "auth-helpers" package.json</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` lists `@supabase/ssr` and does NOT list `@supabase/auth-helpers-nextjs`
    - `components.json` exists at project root (shadcn initialized)
    - `src/components/ui/` contains button, input, form, card, badge, alert, dialog, select, sonner component files
    - `.env.example` documents NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY (no NEXT_PUBLIC_ prefix), SUPABASE_ACCESS_TOKEN
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Next.js app boots with `next dev`, all Phase 1 runtime deps installed at verified versions, shadcn/ui initialized with required components.</done>
</task>

<task type="auto">
  <name>Task 2: Create the three Supabase client factories</name>
  <files>src/lib/supabase/server.ts, src/lib/supabase/client.ts, src/lib/supabase/admin.ts</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 5: createServerClient in Server Components — exact code for server.ts and admin.ts)
    - CLAUDE.md (Do not expose service_role key client-side)
  </read_first>
  <action>
    Create `src/lib/supabase/server.ts` exporting `createClient()` using `createServerClient` from `@supabase/ssr`, reading cookies via `next/headers` `cookies()`, with `getAll`/`setAll` cookie handlers and the try/catch around `setAll` for Server Component contexts (per RESEARCH Pattern 5). Create `src/lib/supabase/client.ts` exporting `createClient()` using `createBrowserClient` from `@supabase/ssr` with `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY`. Create `src/lib/supabase/admin.ts` exporting `createAdminClient()` using `createClient` from `@supabase/supabase-js` with `SUPABASE_SERVICE_ROLE_KEY` (NOT NEXT_PUBLIC_), `auth: { autoRefreshToken: false, persistSession: false }`. Add a top-of-file comment in admin.ts: "SERVER ONLY — never import in 'use client' files". All three should reference a `Database` type from `@/types/supabase` (the type file will be generated in plan 02; use a placeholder import that resolves to `any` until then, e.g. a stub `src/types/supabase.ts` exporting `export type Database = any`).
  </action>
  <verify>
    <automated>grep -q "createServerClient" src/lib/supabase/server.ts && grep -q "createBrowserClient" src/lib/supabase/client.ts && grep -q "SUPABASE_SERVICE_ROLE_KEY" src/lib/supabase/admin.ts && ! grep -rq "NEXT_PUBLIC_SUPABASE_SERVICE_ROLE_KEY" src/</automated>
  </verify>
  <acceptance_criteria>
    - `server.ts` imports `createServerClient` from `@supabase/ssr` and `cookies` from `next/headers`
    - `client.ts` imports `createBrowserClient` from `@supabase/ssr`
    - `admin.ts` reads `process.env.SUPABASE_SERVICE_ROLE_KEY` and contains a "SERVER ONLY" comment
    - No file in `src/` contains the string `NEXT_PUBLIC_SUPABASE_SERVICE_ROLE_KEY`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Three Supabase client factories exist with correct imports; service_role key is server-only with no NEXT_PUBLIC_ prefix anywhere.</done>
</task>

<task type="auto">
  <name>Task 3: Set up test framework, test client fixture, and RLS CI linter (Wave 0 gaps)</name>
  <files>vitest.config.ts, playwright.config.ts, tests/helpers/supabase-test-client.ts, scripts/check-rls.ts</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Validation Architecture section — test commands, Wave 0 Gaps list; Pattern 4: RLS CI Linter — query and check-rls.ts shape)
  </read_first>
  <action>
    Install dev deps: `vitest`, `@playwright/test`. Create `vitest.config.ts` with a node test environment and a `tests/` include glob. Create `playwright.config.ts` with a baseURL of `http://localhost:3000` (OAuth E2E may be deferred but config must exist). Create `tests/helpers/supabase-test-client.ts` exporting a `createTestClient(role)` fixture that returns an authenticated Supabase client for a given role using the anon key plus a signed-in test session (use `createClient` from `@supabase/supabase-js`; sign in with a per-role test user email/password read from env `TEST_<ROLE>_EMAIL`/`TEST_<ROLE>_PASSWORD`). Create `scripts/check-rls.ts` per RESEARCH Pattern 4: connect with the admin client, query `pg_tables WHERE schemaname = 'public' AND NOT rowsecurity`, print any offending table names, and `process.exit(1)` if the result set is non-empty, else exit 0. Use `grep -v '^#'`-safe logic — the script asserts on query rows, not file text. Add npm scripts: `"test": "vitest run"`, `"check:rls": "tsx scripts/check-rls.ts"` (install `tsx` as a dev dep).
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | grep -qiE "no test files|passed|0 passed" && grep -q "rowsecurity" scripts/check-rls.ts && grep -q "process.exit(1)" scripts/check-rls.ts</automated>
  </verify>
  <acceptance_criteria>
    - `vitest.config.ts` and `playwright.config.ts` exist at project root
    - `npx vitest run` exits 0 (no test files yet is acceptable)
    - `tests/helpers/supabase-test-client.ts` exports a per-role authenticated client factory
    - `scripts/check-rls.ts` queries `pg_tables` for `NOT rowsecurity` and calls `process.exit(1)` when offending rows exist
    - `package.json` has `test` and `check:rls` scripts
  </acceptance_criteria>
  <done>Vitest runs clean, Playwright config exists, per-role test client fixture exists, RLS linter script exists and gates on missing-RLS tables.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Confirm Supabase project region and credentials</name>
  <what-built>Project scaffold, three Supabase clients, test harness, and RLS linter are in place. They require a real Supabase project to connect to.</what-built>
  <how-to-verify>
    1. Confirm a Supabase project exists and was created in the `Canada (Central)` / `ca-central-1` region. In Supabase Dashboard -> Project Settings -> General, verify Region reads "Canada (Central)". If the project is in any other region, STOP — region cannot be changed; a new ca-central-1 project must be created for PIPEDA compliance.
    2. Confirm `.env.local` contains real values for NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, and SUPABASE_ACCESS_TOKEN.
    3. Run `next dev` and confirm the default page loads at http://localhost:3000 with no console errors.
  </how-to-verify>
  <resume-signal>Type "approved" once the project is confirmed in ca-central-1 with credentials set, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → app bundle | service_role key must never cross into the client bundle |
| app → Supabase | all DB access authenticated; admin client bypasses RLS and is server-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | src/lib/supabase/admin.ts | mitigate | `SUPABASE_SERVICE_ROLE_KEY` without NEXT_PUBLIC_ prefix; server-only comment; verify grep finds no NEXT_PUBLIC_SUPABASE_SERVICE_ROLE_KEY in src/ |
| T-01-02 | Compliance | Supabase project region | mitigate | Blocking human checkpoint confirms ca-central-1 before any data is written (region immutable) |
| T-01-SC | Tampering | npm installs | mitigate | All packages [Approved] in RESEARCH Package Legitimacy Audit; pinned to verified versions |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run` exits 0
- `grep -rq "NEXT_PUBLIC_SUPABASE_SERVICE_ROLE_KEY" src/` returns nothing
- `grep -q "auth-helpers" package.json` returns nothing
- Supabase project confirmed in ca-central-1
</verification>

<success_criteria>
Next.js 15 app boots, three correct Supabase clients exist, test harness and RLS linter are in place, shadcn/ui initialized, and the Supabase project is confirmed in ca-central-1 with credentials configured.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done
</output>
