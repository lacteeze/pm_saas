---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/middleware.ts
  - src/app/(auth)/layout.tsx
  - src/app/(manager)/layout.tsx
  - src/app/(manager)/dashboard/page.tsx
  - src/app/(tenant)/layout.tsx
  - src/app/(tenant)/my-home/page.tsx
  - src/app/(owner)/layout.tsx
  - src/app/(owner)/portfolio/page.tsx
  - src/app/(vendor)/layout.tsx
  - src/app/(vendor)/jobs/page.tsx
  - src/app/(admin)/layout.tsx
  - src/app/(admin)/admin/page.tsx
  - src/components/layout/ManagerShell.tsx
  - src/components/layout/TenantShell.tsx
  - src/components/layout/OwnerShell.tsx
  - src/components/layout/VendorShell.tsx
  - docs/supabase-auth-config.md
autonomous: false
requirements: [FOUND-02, FOUND-03, FOUND-04, FOUND-07, FOUND-08, FOUND-09, FOUND-10, FOUND-11, FOUND-12, FOUND-14]

must_haves:
  truths:
    - "Middleware refreshes the session on every request and redirects unauthenticated users to /login"
    - "Middleware redirects each role to its own portal and blocks cross-role access"
    - "Each portal route group has its own layout shell with role-appropriate nav"
    - "The admin route group verifies role=admin server-side, not via shared middleware alone"
    - "Supabase Auth is configured for Google, Apple, magic link, 7-day session, and private-only Realtime"
  artifacts:
    - path: "src/middleware.ts"
      provides: "session refresh + role-based routing"
      contains: "getUser"
    - path: "src/app/(admin)/layout.tsx"
      provides: "admin layout with independent server-side role check (FOUND-07)"
      contains: "admin"
    - path: "src/components/layout/ManagerShell.tsx"
      provides: "manager portal sidebar shell"
      min_lines: 20
    - path: "docs/supabase-auth-config.md"
      provides: "checklist of dashboard auth settings (OAuth, session, Realtime private)"
      contains: "private"
  key_links:
    - from: "src/middleware.ts"
      to: "user.app_metadata.role"
      via: "role read from refreshed session"
      pattern: "app_metadata"
    - from: "src/app/(admin)/layout.tsx"
      to: "createClient (server)"
      via: "server-side role verification"
      pattern: "getUser"
---

<objective>
Build the session/routing security layer and empty portal shells: the `@supabase/ssr` middleware (session refresh + role-based redirects), the five portal route groups with layout shells, the separately-guarded admin group, and a documented checklist of Supabase Auth dashboard settings (OAuth providers, 7-day session, private-only Realtime).

Purpose: Middleware is the single entry point for session management; the admin portal must never share middleware with single-org portals (Pitfall 6); Realtime must be private-only before any Realtime feature ships (FOUND-14). This plan wires routing without yet building auth forms (plan 05).
Output: Working middleware, five portal shells, admin isolation, and an auth-config checklist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-UI-SPEC.md

<interfaces>
<!-- src/lib/supabase/server.ts: createClient() (plan 01) -->
<!-- JWT app_metadata.role values: admin, manager, employee, tenant, owner, vendor -->
<!-- Redirect map (D-04): manager/employee -> /dashboard, tenant -> /my-home, owner -> /portfolio, vendor -> /jobs, admin -> /admin -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build middleware with session refresh and role-based routing</name>
  <files>src/middleware.ts</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 1: middleware exact code; Pitfall 6: admin isolation)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-04 redirect map, D-06 single login URL)
  </read_first>
  <action>
    Create `src/middleware.ts` per RESEARCH Pattern 1: build `createServerClient` from `@supabase/ssr` with `getAll`/`setAll` cookie handlers wired to request/response, call `await supabase.auth.getUser()` (CRITICAL — refreshes the session; do not remove). Read `user?.app_metadata?.role`. If no user and path is protected, redirect to `/login`. Enforce per-portal role guards per D-04: `/dashboard` requires manager|employee|admin; `/my-home` requires tenant; `/portfolio` requires owner; `/jobs` requires vendor; `/admin` requires admin (FOUND-07/08/09/10/11/12). Export `config.matcher` excluding `_next/static`, `_next/image`, `favicon.ico`. The admin guard here is a first layer ONLY — the (admin) layout adds an independent server-side check (Pitfall 6).
  </action>
  <verify>
    <automated>grep -q "getUser" src/middleware.ts && grep -q "app_metadata" src/middleware.ts && grep -q "matcher" src/middleware.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - middleware calls `supabase.auth.getUser()` (session refresh)
    - unauthenticated access to a protected path redirects to `/login`
    - each portal path is guarded by the correct role set per D-04
    - `config.matcher` excludes static assets
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Middleware refreshes sessions and enforces role-based routing for all five portals plus admin.</done>
</task>

<task type="auto">
  <name>Task 2: Build portal route groups and layout shells</name>
  <files>src/app/(auth)/layout.tsx, src/app/(manager)/layout.tsx, src/app/(manager)/dashboard/page.tsx, src/app/(tenant)/layout.tsx, src/app/(tenant)/my-home/page.tsx, src/app/(owner)/layout.tsx, src/app/(owner)/portfolio/page.tsx, src/app/(vendor)/layout.tsx, src/app/(vendor)/jobs/page.tsx, src/app/(admin)/layout.tsx, src/app/(admin)/admin/page.tsx, src/components/layout/ManagerShell.tsx, src/components/layout/TenantShell.tsx, src/components/layout/OwnerShell.tsx, src/components/layout/VendorShell.tsx</files>
  <read_first>
    - .planning/phases/01-foundation/01-UI-SPEC.md (Section 3: Portal Shells — structure, nav items per role, empty-state copy; Color/Typography/Spacing tokens; Section: Responsive Breakpoints)
    - .planning/phases/01-foundation/01-RESEARCH.md (Recommended Project Structure; Pitfall 6 admin isolation)
  </read_first>
  <action>
    Create the `(auth)` layout as a centered card layout (warm white bg #FAFAF9, max-width per UI-SPEC). Create four shell components in `src/components/layout/`: ManagerShell (nav: Dashboard, Properties, People, Leases, Maintenance, Payments, Settings), TenantShell (Home, Pay Rent, Maintenance, My Lease), OwnerShell (Portfolio, Statements, Maintenance), VendorShell (My Jobs). Each shell: desktop left sidebar 240px (stone-100 #F5F4F2), mobile bottom tab bar, top org logo + name, bottom user avatar dropdown, content area 16px mobile / 32px desktop padding, max-width 1280px. Wire each `(role)/layout.tsx` to render its shell. Each landing page (dashboard, my-home, portfolio, jobs, admin) renders the empty state: heading "{Feature} coming soon", body "This section is being built. Check back in the next update." For `(admin)/layout.tsx`: add an INDEPENDENT server-side check — call `createClient()` (server), `getUser()`, and if `user.app_metadata.role !== 'admin'` redirect to /login or throw to error boundary (Pitfall 6 — do NOT rely on middleware alone). Use only weights 400/600, accent #D97706 reserved per UI-SPEC, 44px min touch targets.
  </action>
  <verify>
    <automated>test -f "src/app/(admin)/layout.tsx" && grep -q "getUser" "src/app/(admin)/layout.tsx" && grep -q "admin" "src/app/(admin)/layout.tsx" && grep -q "coming soon" "src/app/(manager)/dashboard/page.tsx" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All five portal route groups exist with layout + landing page
    - The four shells render role-correct nav items per UI-SPEC Section 3
    - `(admin)/layout.tsx` performs an independent server-side `role === 'admin'` check (not just middleware)
    - Landing pages render the "coming soon" empty state copy verbatim
    - Sidebar uses stone-100, accent #D97706 only on reserved elements, 44px touch targets
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Five portal shells render with correct nav and empty states; admin group is independently guarded.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Configure Supabase Auth dashboard settings</name>
  <what-built>A documented checklist (docs/supabase-auth-config.md) of the dashboard settings that cannot be set via migration: OAuth providers, session duration, and private-only Realtime.</what-built>
  <how-to-verify>
    1. Create `docs/supabase-auth-config.md` listing each setting below as a checklist.
    2. Google OAuth (FOUND-02): Supabase Dashboard -> Authentication -> Providers -> Google: enable, paste OAuth client ID/secret from Google Cloud Console; add callback `https://<project>.supabase.co/auth/v1/callback` to Google authorized redirect URIs.
    3. Apple OAuth (FOUND-03): Authentication -> Providers -> Apple: enable with Services ID + key from Apple Developer.
    4. Magic link / SMTP (FOUND-04): Authentication -> Email -> configure Resend as custom SMTP using RESEND_API_KEY.
    5. Session (D-10): Authentication -> Sessions/JWT -> set refresh token reuse interval / inactivity timeout to 7 days (verify behavior per RESEARCH Open Question 4).
    6. Realtime private-only (FOUND-14): Project Settings -> Realtime -> enable "Private channels only".
    7. Confirm each box is checked. If Google/Apple credentials are unavailable, note which are deferred (email/password + magic link must work regardless).
  </how-to-verify>
  <resume-signal>Type "approved" once dashboard auth settings are configured (or note which OAuth providers are deferred pending credentials), and Realtime private-only is enabled.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| request → middleware | session validity + role gate before any page renders |
| admin routes → DB | cross-org access; must not share guard with single-org portals |
| Realtime channel → org | private-only prevents cross-org event leakage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | role-based routing | mitigate | Middleware role guard per D-04 on every protected path |
| T-04-02 | Information Disclosure | (admin) route group | mitigate | Independent server-side role check in (admin)/layout.tsx (Pitfall 6) |
| T-04-03 | Information Disclosure | Supabase Realtime | mitigate | Private-only channels enabled in project settings (FOUND-14) |
| T-04-04 | Spoofing | OAuth callback | mitigate | Callback URLs allowlisted in Supabase + Google/Apple consoles |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- middleware redirects unauthenticated -> /login and enforces role guards
- (admin) layout independently verifies role
- docs/supabase-auth-config.md checklist complete; Realtime private-only enabled
</verification>

<success_criteria>
Middleware enforces session + role routing, five portal shells render with correct nav and empty states, admin is independently guarded, and Supabase Auth (OAuth, session, private Realtime) is configured.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-04-SUMMARY.md` when done
</output>
