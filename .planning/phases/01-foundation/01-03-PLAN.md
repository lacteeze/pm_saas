---
phase: 01-foundation
plan: 03
type: tdd
wave: 3
depends_on: ["01-02"]
files_modified:
  - tests/rls/cross-org-isolation.test.ts
  - tests/auth/jwt-claims.test.ts
  - tests/rls/admin-access.test.ts
  - tests/rls/manager-permissions.test.ts
  - tests/rls/employee-permissions.test.ts
  - tests/rls/tenant-isolation.test.ts
  - tests/rls/owner-isolation.test.ts
  - tests/rls/vendor-isolation.test.ts
  - tests/orgs/plan-limits.test.ts
  - tests/helpers/seed.ts
autonomous: true
requirements: [FOUND-05, FOUND-06, FOUND-07, FOUND-08, FOUND-09, FOUND-10, FOUND-11, FOUND-12, ORGS-05, ORGS-06]

must_haves:
  truths:
    - "A user authenticated as Org A cannot read, write, or enumerate any Org B data"
    - "A signed-in user's JWT contains role, org_id, and person_id in app_metadata"
    - "An admin can read across all orgs; a manager cannot"
    - "A manager can CRUD rows within their own org"
    - "An employee can only read/write data within their assigned org scope (FOUND-09)"
    - "Within one org, a tenant cannot read another tenant's row; an owner cannot read another owner's row; a vendor cannot read another vendor's row (FOUND-10/11/12)"
    - "Inserting a unit beyond plan_unit_limit is rejected by the DB trigger (ORGS-06)"
  artifacts:
    - path: "tests/rls/cross-org-isolation.test.ts"
      provides: "the critical multi-tenant isolation test (FOUND-05)"
      contains: "cross-org"
    - path: "tests/auth/jwt-claims.test.ts"
      provides: "verifies Auth Hook injects claims (FOUND-06)"
      contains: "app_metadata"
    - path: "tests/rls/employee-permissions.test.ts"
      provides: "employee scoped CRUD assertions (FOUND-09)"
      contains: "employee"
    - path: "tests/rls/tenant-isolation.test.ts"
      provides: "within-org tenant-vs-tenant isolation (FOUND-10)"
      contains: "tenant"
    - path: "tests/rls/owner-isolation.test.ts"
      provides: "within-org owner-vs-owner isolation (FOUND-11)"
      contains: "owner"
    - path: "tests/rls/vendor-isolation.test.ts"
      provides: "within-org vendor-vs-vendor isolation (FOUND-12)"
      contains: "vendor"
    - path: "tests/orgs/plan-limits.test.ts"
      provides: "asserts the DB trigger rejects an over-limit unit INSERT (ORGS-06)"
      contains: "plan_unit_limit"
    - path: "tests/helpers/seed.ts"
      provides: "two-org seed fixture for isolation tests"
      contains: "organizations"
  key_links:
    - from: "tests/rls/cross-org-isolation.test.ts"
      to: "tests/helpers/supabase-test-client.ts"
      via: "per-role authenticated client"
      pattern: "createTestClient"
    - from: "tests/orgs/plan-limits.test.ts"
      to: "public.units enforce_plan_unit_limit trigger"
      via: "INSERT beyond limit expected to error"
      pattern: "plan_unit_limit"
---

<objective>
Prove the security boundary works with automated tests before any UI is built. This is the highest-value verification in Phase 1: cross-org isolation, JWT claim injection, admin cross-org access, manager + employee scoped permissions, within-org cross-user isolation for tenant/owner/vendor, and DB-enforced plan-limit rejection.

Purpose: RESEARCH mandates "do not build any UI before RLS is verified by automated test." A passing isolation suite is the gate that proves the Auth Hook, RLS policies, and the plan-limit trigger from plan 02 actually work against the live DB — not just that the SQL parsed.
Output: Nine test suites + a two-org seed fixture, all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/STATE.md

<interfaces>
<!-- From tests/helpers/supabase-test-client.ts (plan 01): createTestClient(role) -> authenticated client -->
<!-- Tables: public.organizations, public.people, public.units (see src/types/supabase.ts) -->
<!-- JWT app_metadata: role, org_id, person_id -->
<!-- plan 02 trigger: enforce_plan_unit_limit() BEFORE INSERT on public.units raises check_violation when count >= organizations.plan_unit_limit -->
</interfaces>
</context>

<feature>
  <name>RLS + Auth Hook + plan-limit verification suite</name>
  <files>tests/helpers/seed.ts, tests/rls/cross-org-isolation.test.ts, tests/auth/jwt-claims.test.ts, tests/rls/admin-access.test.ts, tests/rls/manager-permissions.test.ts, tests/rls/employee-permissions.test.ts, tests/rls/tenant-isolation.test.ts, tests/rls/owner-isolation.test.ts, tests/rls/vendor-isolation.test.ts, tests/orgs/plan-limits.test.ts</files>
  <behavior>
    - Seed: create two orgs (Org A, Org B). In EACH org seed one manager, one employee, two tenants (tenant1/tenant2), two owners (owner1/owner2), two vendors (vendor1/vendor2), and seed one platform admin. Expose all ids and credentials. (The second tenant/owner/vendor per org enables within-org cross-user assertions.)
    - cross-org-isolation: a client authenticated as Org A's manager SELECTing people/organizations/units returns ONLY Org A rows; an attempt to UPDATE/DELETE an Org B row affects 0 rows or errors (FOUND-05).
    - jwt-claims: after sign-in, decoding the access token shows `app_metadata.role`, `app_metadata.org_id`, `app_metadata.person_id` populated and matching the seeded person (FOUND-06).
    - admin-access: admin client SELECT on organizations returns BOTH orgs; manager client returns only one (FOUND-07).
    - manager-permissions: manager can INSERT/UPDATE/DELETE a person within own org; the row persists and is readable back (FOUND-08).
    - employee-permissions (FOUND-09): an employee client can read/write people + units rows within its OWN org (scoped CRUD succeeds), but SELECT/UPDATE of an Org B row returns 0 rows / error. Asserts the employee is org-scoped, not cross-org.
    - tenant-isolation (FOUND-10): tenant1 (Org A) client SELECTing the people table cannot read tenant2's row within the same Org A (returns only its own self-row); a cross-org read of any Org B row returns 0.
    - owner-isolation (FOUND-11): owner1 (Org A) client cannot read owner2's row within the same Org A; self-row only.
    - vendor-isolation (FOUND-12): vendor1 (Org A) client cannot read vendor2's row within the same Org A; self-row only.
    - plan-limits (ORGS-05/06): set Org A plan_unit_limit = 2 via admin client. Using a manager client, INSERT 2 units into public.units (both succeed). The 3rd INSERT MUST be rejected by the DB trigger — assert the call returns an error (Postgres check_violation / message includes "plan_unit_limit"). After lowering count or raising the limit, an INSERT succeeds again. This asserts the INSERT is actually rejected at the DB layer, not merely that the limit field exists.
  </behavior>
  <implementation>
    RED: write seed.ts (two orgs, multiple users per role per org) plus all nine test files against the live remote DB using createTestClient per role/user. Run `npx vitest run tests/` — they should pass against the correct plan-02 schema; any failure indicates a real RLS/hook/trigger defect in plan 02 to fix, not a test bug. For plan-limits specifically, wrap the over-limit INSERT in expect-to-reject (the supabase-js call returns `{ error }`; assert error is non-null and references the limit). GREEN: confirm the suite is green. REFACTOR: extract shared seed + assertion helpers into seed.ts. Commit RED then GREEN.
  </implementation>
</feature>

<verification>
- `npx vitest run tests/rls/ tests/auth/ tests/orgs/` exits 0
- cross-org-isolation explicitly asserts Org A client sees 0 Org B rows
- jwt-claims asserts all three app_metadata fields present
- employee-permissions asserts in-org write succeeds AND cross-org read returns 0 (FOUND-09)
- tenant/owner/vendor isolation each assert a within-org cross-user read returns 0 rows (FOUND-10/11/12)
- plan-limits asserts the 3rd (over-limit) unit INSERT returns an error (ORGS-06)
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test client JWT → RLS | tests exercise the real isolation boundary |
| test client INSERT → plan-limit trigger | tests exercise the real DB-layer rejection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | cross-org RLS | mitigate | Automated test asserts Org A cannot read/write Org B; gates phase |
| T-03-02 | Elevation of Privilege | JWT claims | mitigate | Test asserts claims match seeded person, not client-supplied values |
| T-03-03 | Information Disclosure | within-org cross-user reads | mitigate | tenant/owner/vendor isolation tests assert self-row-only access (FOUND-10/11/12) |
| T-03-04 | Elevation of Privilege | plan-limit bypass | mitigate | plan-limits test asserts over-limit INSERT is rejected at DB layer (ORGS-06) |
</threat_model>

<success_criteria>
All nine suites pass against the live ca-central-1 DB, proving cross-org isolation, JWT claim injection, admin cross-org access, manager + employee scoped CRUD, within-org tenant/owner/vendor isolation, and DB-enforced plan-limit rejection.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done
</output>
</content>
