---
phase: 01-foundation
plan: 05
type: execute
wave: 4
depends_on: ["01-04"]
files_modified:
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/signup/page.tsx
  - src/components/auth/SignInForm.tsx
  - src/components/auth/MagicLinkForm.tsx
  - src/components/auth/OAuthButtons.tsx
  - src/app/auth/callback/route.ts
  - src/app/auth/confirm/route.ts
  - src/app/(auth)/auth-code-error/page.tsx
  - src/app/onboarding/page.tsx
  - src/app/onboarding/actions.ts
  - src/components/onboarding/WizardShell.tsx
  - src/components/onboarding/steps/OrgNameStep.tsx
  - src/components/onboarding/steps/LogoStep.tsx
  - src/components/onboarding/steps/ProvinceStep.tsx
  - src/components/onboarding/steps/InviteStep.tsx
  - src/lib/validation/auth.ts
  - src/lib/constants/provinces.ts
autonomous: true
requirements: [FOUND-01, FOUND-02, FOUND-03, FOUND-04, ORGS-04]

must_haves:
  truths:
    - "A user can sign in with email/password, Google, Apple, or magic link from one /login page"
    - "After auth, the user is redirected to their role's portal per D-04"
    - "A new user can create an org through the 5-step onboarding wizard"
    - "Org name and province are required; logo and first-manager invite are skippable"
    - "Skipping a step leaves setup_completed_at null so the setup banner can appear"
  artifacts:
    - path: "src/components/auth/SignInForm.tsx"
      provides: "email/password + magic-link toggle (D-04, D-06)"
      min_lines: 30
    - path: "src/app/auth/callback/route.ts"
      provides: "OAuth + magic-link code exchange with role-based redirect"
      contains: "exchangeCodeForSession"
    - path: "src/app/onboarding/actions.ts"
      provides: "Server Action creating org + first person (FOUND-01)"
      contains: "use server"
    - path: "src/lib/constants/provinces.ts"
      provides: "13 Canadian provinces/territories"
      contains: "British Columbia"
  key_links:
    - from: "src/app/auth/callback/route.ts"
      to: "redirect map by role"
      via: "user.app_metadata.role"
      pattern: "app_metadata"
    - from: "src/app/onboarding/actions.ts"
      to: "public.organizations + public.people"
      via: "Server Action insert via server client"
      pattern: "from\\('organizations'\\)"
---

<objective>
Build the authentication UI (single sign-in page with all four methods, OAuth/magic-link callback handlers) and the 5-step onboarding wizard that takes a new PM company from sign-up to dashboard.

Purpose: This delivers FOUND-01 through FOUND-04 and the D-01/D-03 onboarding flow. One login page with role-based redirect (D-04/D-06); Canary itself goes through the same flow (D-03, no seed bypass).
Output: Working sign-in (4 methods), callback handlers, and onboarding wizard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-UI-SPEC.md

<interfaces>
<!-- src/lib/supabase/client.ts: createClient() browser; src/lib/supabase/server.ts: createClient() server -->
<!-- Redirect map (D-04): manager/employee->/dashboard, tenant->/my-home, owner->/portfolio, vendor->/jobs, admin->/admin -->
<!-- organizations columns: name, slug, province, logo_path, setup_completed_at -->
<!-- people columns: user_id, org_id, role, email -->
<!-- CANADIAN_PROVINCES shape from RESEARCH Code Examples (value/label) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build sign-in page (4 methods) and callback handlers</name>
  <files>src/app/(auth)/login/page.tsx, src/components/auth/SignInForm.tsx, src/components/auth/MagicLinkForm.tsx, src/components/auth/OAuthButtons.tsx, src/app/auth/callback/route.ts, src/app/auth/confirm/route.ts, src/app/(auth)/auth-code-error/page.tsx, src/lib/validation/auth.ts</files>
  <read_first>
    - .planning/phases/01-foundation/01-UI-SPEC.md (Section 1: Sign-In Page — layout, interaction flow, loading states, magic link flow; Error States & Copy; Copywriting Contract)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 6: OAuth callback route exact code; Don't Hand-Roll — use Supabase Auth for OAuth/magic link/passwords)
  </read_first>
  <behavior>
    - SignInForm: valid email/password calls `supabase.auth.signInWithPassword`; on success the browser navigates and middleware/callback redirects by role.
    - Invalid credentials shows UI-SPEC copy "Couldn't sign you in" — no raw error codes.
    - MagicLinkForm: submitting an email calls `signInWithOtp` and shows "Check your email" confirmation.
    - OAuthButtons: Google/Apple call `signInWithOAuth` with redirectTo `/auth/callback`.
    - callback route: exchanges code, reads role from app_metadata, redirects via the D-04 map; on error redirects to /auth-code-error.
  </behavior>
  <action>
    Create `src/lib/validation/auth.ts` with Zod schemas (email, password min length) used by both client (`zodResolver`) and any server use — single schema (Don't Hand-Roll). Build `SignInForm` (shadcn Form/Input/Button, password show/hide via Eye/EyeOff, 44px buttons, accent #D97706 CTA, autofocus email), `MagicLinkForm` (email-only, replaces password UI on "Send me a magic link" toggle), `OAuthButtons` (Google white+border, Apple black, both 44px, "Redirecting to {provider}..." loading text). Compose them in `(auth)/login/page.tsx` with "Or" separator and "Create one" link to /signup. Implement `auth/callback/route.ts` per RESEARCH Pattern 6 (exchangeCodeForSession + D-04 role redirect map). Implement `auth/confirm/route.ts` for magic-link/email OTP confirmation. Create `(auth)/auth-code-error/page.tsx` with UI-SPEC "Sign-in didn't complete" copy. Use only Supabase Auth methods — never hand-roll OAuth/password/magic-link.
  </action>
  <verify>
    <automated>grep -q "exchangeCodeForSession" "src/app/auth/callback/route.ts" && grep -q "signInWithPassword" src/components/auth/SignInForm.tsx && grep -q "signInWithOAuth" src/components/auth/OAuthButtons.tsx && grep -q "Couldn't sign you in" src/components/auth/SignInForm.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `/login` renders email/password, Google, Apple, and magic-link entry points (FOUND-01/02/03/04)
    - callback route exchanges the code and redirects by role per D-04
    - invalid-credential and OAuth-error copy matches UI-SPEC verbatim (no raw codes)
    - validation uses a single Zod schema via zodResolver
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Single sign-in page supports all four auth methods with role-based redirect and UI-SPEC error copy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the 5-step onboarding wizard and org-creation Server Action</name>
  <files>src/app/(auth)/signup/page.tsx, src/app/onboarding/page.tsx, src/app/onboarding/actions.ts, src/components/onboarding/WizardShell.tsx, src/components/onboarding/steps/OrgNameStep.tsx, src/components/onboarding/steps/LogoStep.tsx, src/components/onboarding/steps/ProvinceStep.tsx, src/components/onboarding/steps/InviteStep.tsx, src/lib/constants/provinces.ts</files>
  <read_first>
    - .planning/phases/01-foundation/01-UI-SPEC.md (Section 2: Onboarding Wizard — all 5 steps, validation, skip behavior, Progress/step counter)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-01 required fields, D-02 setup banner, D-03 Canary uses same flow)
    - .planning/phases/01-foundation/01-RESEARCH.md (Code Examples -> CANADIAN_PROVINCES; Architectural Responsibility Map -> org sign-up = Server Actions)
  </read_first>
  <behavior>
    - OrgNameStep: required, 2..80 chars; Continue blocked when invalid.
    - ProvinceStep: required selection from 13 provinces; Continue blocked when empty.
    - LogoStep and InviteStep: skippable ("Skip for now").
    - Completing all steps sets organizations.setup_completed_at; skipping any leaves it null (so D-02 banner can show).
    - createOrg Server Action inserts organizations + a manager people row tied to the signed-up user (FOUND-01).
  </behavior>
  <action>
    Create `src/lib/constants/provinces.ts` with the 13 CANADIAN_PROVINCES (value/label) from RESEARCH. Build `WizardShell` with shadcn `Progress` + "Step N of 5" counter (14px), active step circle in accent #D97706. Build the four step components per UI-SPEC Section 2 copy and validation (react-hook-form + Zod). Step 5 is the completion screen ("You're all set, {org name}!" -> "Go to dashboard"). `(auth)/signup/page.tsx` is the wizard entry (email/password account creation via `supabase.auth.signUp`), then `onboarding/page.tsx` runs the wizard (outside portal groups — runs before org fully exists). `onboarding/actions.ts` ('use server'): a `createOrganization` action validating input with Zod, inserting into `organizations` (name, slug, province, logo_path) and a `people` row (role='manager', user_id = current user, org_id), setting `setup_completed_at` only when no steps skipped. LogoStep uploads to the `org-assets` bucket under `/{org_id}/` (ORGS-04). Logo upload client-side (Responsibility Map: Browser/Client → Supabase Storage), mutations server-side via Server Action. D-03: no seed bypass — Canary uses this exact flow.
  </action>
  <verify>
    <automated>grep -q "use server" src/app/onboarding/actions.ts && grep -q "from('organizations')" src/app/onboarding/actions.ts && grep -q "British Columbia" src/lib/constants/provinces.ts && grep -q "setup_completed_at" src/app/onboarding/actions.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Wizard has 5 steps; org name + province required, logo + invite skippable (D-01)
    - `createOrganization` Server Action inserts org + manager person row (FOUND-01)
    - `setup_completed_at` set only when no step skipped (D-02 banner precondition)
    - provinces.ts lists all 13 Canadian provinces/territories
    - no hardcoded Canary seed bypass (D-03)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Onboarding wizard creates an org + first manager via Server Action; required/skippable fields per D-01; setup completion tracked per D-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| login form → Supabase Auth | credentials handled by Supabase, never stored/hashed in-app |
| OAuth callback → session | code exchange server-side; no open redirect |
| onboarding form → Server Action | org/person mutations server-side, Zod-validated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Spoofing | OAuth callback route | mitigate | exchangeCodeForSession server-side; redirect map only to known internal paths |
| T-05-02 | Information Disclosure | auth error copy | mitigate | UI-SPEC copy only; no raw error codes/stack traces to user (V7) |
| T-05-03 | Tampering | createOrganization action | mitigate | Zod validation on all inputs server-side (V5); RLS enforces org scope |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- /login offers all four auth methods; callback redirects by role
- onboarding wizard creates org + manager person; required fields enforced
</verification>

<success_criteria>
A new user signs up, completes the wizard (org name + province required), and lands on /dashboard; existing users sign in via any of the four methods with correct role redirect and UI-SPEC error copy.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-05-SUMMARY.md` when done
</output>
