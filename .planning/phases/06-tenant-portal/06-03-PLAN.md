---
phase: 06-tenant-portal
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - canary-propos/src/app/(manager)/properties/[id]/AnnouncementsSection.tsx
  - canary-propos/src/app/(manager)/properties/[id]/page.tsx
  - canary-propos/src/app/(tenant)/my-home/announcements/page.tsx
  - canary-propos/src/app/(tenant)/my-home/announcements/actions.ts
autonomous: true
requirements:
  - TENANT-06
must_haves:
  truths:
    - "Manager can post an announcement from /properties/[id] with a title, body, and optional expiry date"
    - "Tenant sees all non-expired announcements for their property at /my-home/announcements, newest first"
    - "Visiting /my-home/announcements updates last_seen_announcements_at on the tenant's people row"
    - "Expired announcements (expires_at < now()) are excluded from the tenant view"
    - "No vendor_cost or billed_amount fields appear in any query in this plan"
  artifacts:
    - path: "canary-propos/src/app/(manager)/properties/[id]/AnnouncementsSection.tsx"
      provides: "Manager announcement creation and list UI"
    - path: "canary-propos/src/app/(tenant)/my-home/announcements/page.tsx"
      provides: "Tenant announcements feed (RSC) — updates last_seen on load"
    - path: "canary-propos/src/app/(tenant)/my-home/announcements/actions.ts"
      provides: "Server Actions: createAnnouncement, markAnnouncementsSeen"
  key_links:
    - from: "announcements page load"
      to: "markAnnouncementsSeen action"
      via: "RSC calls action on render to update last_seen_announcements_at"
    - from: "announcements.property_id"
      to: "tenant's active lease → unit → property_id"
      via: "RLS join — enforced at DB layer"
---

<objective>
Build the announcements feature: manager posts announcements from /properties/[id]; tenant reads them at /my-home/announcements, which marks them as seen.

Purpose: Delivers TENANT-06 — management announcements feed.
Output: Manager posting UI on property detail page, tenant announcements feed, two server actions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@canary-propos/AGENTS.md
@.planning/phases/06-tenant-portal/06-CONTEXT.md
@.planning/phases/06-tenant-portal/06-01-SUMMARY.md
@canary-propos/src/app/(tenant)/my-home/payments/page.tsx

<interfaces>
<!-- Established patterns -->

Auth + person fetch:
```typescript
const supabase = await createClient()
const { data: { user } } = await supabase.auth.getUser()
if (!user) redirect('/login')
const { data: person } = await supabase.from('people').select('id, roles').eq('user_id', user.id).single()
```

Role check (always includes(), never ===):
```typescript
if (!person.roles.includes('manager')) redirect('/dashboard')
```

org_id from org_members:
```typescript
const { data: membership } = await supabase.from('org_members').select('org_id').eq('user_id', user.id).single()
```

D-09: last_seen_announcements_at is stored on people table (nullable timestamptz). Updated when tenant visits /my-home or announcements page.
D-11: NEVER select vendor_cost or billed_amount in tenant-facing queries.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server Actions for announcements</name>
  <files>canary-propos/src/app/(tenant)/my-home/announcements/actions.ts</files>
  <action>
Create canary-propos/src/app/(tenant)/my-home/announcements/actions.ts as a 'use server' module.

createAnnouncement: Takes propertyId (UUID), title (string), body (string), expiresAt (string | null — ISO date string from a date input). Auth guard: role.includes('manager') or role.includes('employee') — redirect to /dashboard otherwise. Resolve org_id via org_members. Insert into announcements with all fields. If expiresAt is provided, parse to ISO timestamp; otherwise set NULL. Call revalidatePath for /properties/[propertyId].

markAnnouncementsSeen: Takes no arguments — resolves tenant from auth. Auth guard: role.includes('tenant') — return early if not tenant (no redirect, called from RSC). UPDATE people SET last_seen_announcements_at = now() WHERE user_id = auth.uid(). No revalidatePath needed — this is a fire-and-forget side effect.
  </action>
  <verify>
    <automated>cd canary-propos && npx tsc --noEmit 2>&1 | grep -c "announcements/actions"</automated>
  </verify>
  <done>actions.ts exports createAnnouncement and markAnnouncementsSeen; tsc clean</done>
</task>

<task type="auto">
  <name>Task 2: Manager announcements section on /properties/[id]</name>
  <files>
    canary-propos/src/app/(manager)/properties/[id]/AnnouncementsSection.tsx
    canary-propos/src/app/(manager)/properties/[id]/page.tsx
  </files>
  <action>
AnnouncementsSection.tsx — Client component ('use client'). Props: propertyId (string), announcements (array of announcement rows: id, title, body, created_at, expires_at).

Renders:
- "Post Announcement" button that expands an inline form (no Dialog required)
- Form fields: Title (text, required), Message body (textarea, required), Expires (date input, optional — label "Expires on (optional)")
- Submit calls createAnnouncement server action. Button shows loading state during submission.
- Below the form: list of existing announcements, newest first. Each card shows: title, first 120 chars of body as excerpt (truncate with ellipsis), posted date, expiry date if set, and a subtle "Expired" badge if expires_at < now().
- Empty state: "No announcements posted yet for this property."

Fetch in the page RSC: SELECT id, title, body, created_at, expires_at FROM announcements WHERE property_id = params.id ORDER BY created_at DESC LIMIT 50.

Integrate into /properties/[id]/page.tsx: Add an "Announcements" section. Import AnnouncementsSection and pass propertyId and fetched announcements. Do not remove or alter existing sections.

Mobile: form fields and announcement cards must be readable at 375px.
  </action>
  <verify>
    <automated>cd canary-propos && npx tsc --noEmit 2>&1 | grep -c "AnnouncementsSection"</automated>
  </verify>
  <done>AnnouncementsSection renders on /properties/[id]; create form and announcement list functional; tsc clean</done>
</task>

<task type="auto">
  <name>Task 3: Tenant announcements feed page</name>
  <files>canary-propos/src/app/(tenant)/my-home/announcements/page.tsx</files>
  <action>
RSC page. Auth guard, person fetch. Guard: roles.includes('tenant') or redirect('/login').

Step 1 — Derive property_id: SELECT l.id, u.property_id FROM leases l JOIN units u ON u.id = l.unit_id WHERE l.tenant_id = person.id AND l.status = 'active' LIMIT 1. If no active lease: render empty state "No active lease found."

Step 2 — Fetch announcements: SELECT id, title, body, created_at FROM announcements WHERE property_id = derived_property_id AND (expires_at IS NULL OR expires_at > now()) ORDER BY created_at DESC LIMIT 50. The RLS policy provides the same filter at the DB layer; the app-level WHERE is defence-in-depth.

Step 3 — Mark seen: call markAnnouncementsSeen() (imported from ./actions) at the top of the RSC function body, before rendering. This is fire-and-forget; do not await it in a way that blocks render — use void markAnnouncementsSeen() or a parallel await.

Render:
- Page heading "Announcements"
- If no announcements: empty state "No announcements from your property manager yet."
- Announcement cards: title in bold, full body text, formatted posted date (en-CA locale). Cards styled like the payment history rows in payments/page.tsx (border-bottom list or card grid).

D-11: No vendor_cost or billed_amount anywhere in this file.
  </action>
  <verify>
    <automated>cd canary-propos && npx tsc --noEmit 2>&1 | grep -c "announcements/page"</automated>
  </verify>
  <done>/my-home/announcements/page.tsx renders announcement list; calls markAnnouncementsSeen on load; tsc clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant → last_seen UPDATE | Tenant updates own people row; must not update any other row |
| manager → createAnnouncement | Manager must only create announcements for properties in their org |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Information Disclosure | tenant announcements SELECT | mitigate | RLS joins through leases/units to derive property scope — tenant cannot request announcements for arbitrary properties |
| T-06-09 | Tampering | markAnnouncementsSeen UPDATE | mitigate | SQL WHERE user_id = auth.uid() — can only update own row; RLS policy added in 06-01 |
| T-06-10 | Spoofing | createAnnouncement property scope | mitigate | RLS WITH CHECK (org_id = get_org_id()) prevents manager from posting to another org's property |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- npx tsc --noEmit exits 0
- /my-home/announcements shows announcements for tenant's property
- Posting an announcement from /properties/[id] appears immediately after revalidation
- last_seen_announcements_at is updated in the people table on page load
</verification>

<success_criteria>
Manager can post announcements on /properties/[id]. Tenant sees all non-expired announcements for their property at /my-home/announcements. Page load updates last_seen_announcements_at. No vendor_cost or billed_amount in any query.
</success_criteria>

<output>
Create `.planning/phases/06-tenant-portal/06-03-SUMMARY.md` when done
</output>
