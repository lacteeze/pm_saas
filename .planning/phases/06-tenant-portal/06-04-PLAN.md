---
phase: 06-tenant-portal
plan: 04
type: execute
wave: 3
depends_on:
  - 06-02
  - 06-03
files_modified:
  - canary-propos/src/app/(tenant)/my-home/page.tsx
  - canary-propos/src/components/layout/TenantShell.tsx
autonomous: true
requirements:
  - TENANT-01
  - TENANT-02
  - TENANT-03
  - TENANT-04
  - TENANT-05
  - TENANT-06
must_haves:
  truths:
    - "Tenant lands on /my-home and sees five summary cards: Lease, Payments, Maintenance, Checklist, Announcements"
    - "Each card shows a meaningful status snippet and links to its sub-page"
    - "Payments card shows last payment amount + date (no vendor_cost or billed_amount)"
    - "Maintenance card shows count of open (non-closed, non-completed) work orders"
    - "Checklist card shows Pending / Submitted state or Not prepared"
    - "Announcements card shows unread count (announcements since last_seen_announcements_at)"
    - "TenantShell nav includes Checklist and Announcements links"
    - "All six TENANT-0X requirements are reachable from the /my-home landing"
  artifacts:
    - path: "canary-propos/src/app/(tenant)/my-home/page.tsx"
      provides: "Redesigned /my-home landing with 5 summary cards"
    - path: "canary-propos/src/components/layout/TenantShell.tsx"
      provides: "Updated nav with Checklist + Announcements items"
  key_links:
    - from: "Announcements card unread count"
      to: "announcements.created_at > people.last_seen_announcements_at"
      via: "COUNT query in RSC"
    - from: "Checklist card"
      to: "/my-home/checklist"
      via: "Link href"
    - from: "TenantShell NAV_ITEMS"
      to: "/my-home/checklist and /my-home/announcements"
      via: "href entries"
---

<objective>
Redesign the /my-home landing page with five summary cards and update TenantShell navigation to include Checklist and Announcements. This plan also serves as the end-to-end wiring verification for all six TENANT requirements.

Purpose: Delivers D-10 (summary card landing) and completes TENANT-01 through TENANT-06 wiring.
Output: Redesigned /my-home page; updated TenantShell nav.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@canary-propos/AGENTS.md
@.planning/phases/06-tenant-portal/06-CONTEXT.md
@.planning/phases/06-tenant-portal/06-02-SUMMARY.md
@.planning/phases/06-tenant-portal/06-03-SUMMARY.md
@canary-propos/src/app/(tenant)/my-home/page.tsx
@canary-propos/src/components/layout/TenantShell.tsx

<interfaces>
<!-- Current my-home/page.tsx structure to extend -->

Current page fetches: person, active lease (with unit + property join).
New page must additionally fetch: last payment, open work order count, active checklist status, unread announcement count.

TenantShell current NAV_ITEMS (to extend):
```typescript
const NAV_ITEMS = [
  { label: 'Home', href: '/my-home', icon: Home },
  { label: 'Pay Rent', href: '/pay-rent', icon: CreditCard },
  { label: 'Maintenance', href: '/my-home/maintenance', icon: Wrench },
  { label: 'My Lease', href: '/my-lease', icon: FileText },
]
```
Add after Maintenance:
  { label: 'Checklist', href: '/my-home/checklist', icon: ClipboardList }
  { label: 'Announcements', href: '/my-home/announcements', icon: Bell }

D-11: NEVER include vendor_cost or billed_amount in any SELECT in this file.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update TenantShell navigation</name>
  <files>canary-propos/src/components/layout/TenantShell.tsx</files>
  <action>
Add two new nav items to NAV_ITEMS in TenantShell.tsx, after the Maintenance entry:
- { label: 'Checklist', href: '/my-home/checklist', icon: ClipboardList }
- { label: 'Announcements', href: '/my-home/announcements', icon: Bell }

Import ClipboardList and Bell from 'lucide-react' alongside the existing icon imports.

No other changes to TenantShell. The bottom tab bar on mobile uses the same NAV_ITEMS array so both desktop and mobile navigation update automatically.

The mobile bottom bar already has four items. With six items on mobile it will become crowded — keep the labels short (already fine: "Checklist", "Announcements"). The existing flex-1 layout handles this; no layout changes needed.
  </action>
  <verify>
    <automated>cd canary-propos && npx tsc --noEmit 2>&1 | grep -c "TenantShell"</automated>
  </verify>
  <done>TenantShell imports ClipboardList and Bell; NAV_ITEMS has 6 entries including /my-home/checklist and /my-home/announcements; tsc clean</done>
</task>

<task type="auto">
  <name>Task 2: Redesign /my-home landing with summary cards</name>
  <files>canary-propos/src/app/(tenant)/my-home/page.tsx</files>
  <action>
Replace the current single-card layout with a five-card summary dashboard (per D-10). Keep the same auth/person/lease fetching logic at the top.

Add these additional data fetches (all guarded: only run if person and active lease exist):

Payments card data: SELECT amount, payment_date FROM payments WHERE lease_id = lease.id ORDER BY payment_date DESC LIMIT 1. Columns: amount, payment_date. NEVER select vendor_cost or billed_amount. If no payment: show "No payments recorded yet."

Maintenance card data: SELECT COUNT(*) FROM work_orders WHERE property_id = property.id AND created_by = person.id AND status NOT IN ('completed', 'closed'). Show "N open request(s)" or "No open requests."

Checklist card data: SELECT id, title, type, submitted_at FROM checklists WHERE lease_id = lease.id ORDER BY created_at DESC LIMIT 1. Show "Submitted" (with date) if submitted_at set, "Pending sign-off" if exists but not submitted, "Not prepared" if no checklist.

Announcements card data: SELECT COUNT(*) FROM announcements WHERE property_id = property.id AND (expires_at IS NULL OR expires_at > now()) AND created_at > COALESCE(person.last_seen_announcements_at, '1970-01-01'::timestamptz). For last_seen fetch: include last_seen_announcements_at in the person SELECT at top of page. Show "N unread" or "Up to date."

Derive property_id from lease.units.properties (already available from existing lease join) — no extra query needed.

Layout: a responsive card grid. On mobile: single column. On desktop (md+): 2-column grid, or a flex-wrap. Each card:
- Icon (from lucide-react) + section title as the card header
- Status line (the snippet data)
- "View" or action link as a text link or small button at the bottom

Card sections and their hrefs:
1. Lease — icon: FileText — href: /my-home (self, scrolls to lease detail below the cards, or link to PDF download)
2. Payments — icon: CreditCard — href: /my-home/payments
3. Maintenance — icon: Wrench — href: /my-home/maintenance
4. Checklist — icon: ClipboardList — href: /my-home/checklist
5. Announcements — icon: Bell — href: /my-home/announcements (show unread badge number in amber if > 0)

Below the card grid: keep the existing lease detail card (property address, term, rent, PDF download button, Pay Rent button). This preserves TENANT-04 wiring.

Empty state (no active lease): keep the existing dashed-border empty state — just wrap it below a simplified header.

Import ClipboardList and Bell from lucide-react.

D-11 verification: grep the file after writing — confirm vendor_cost and billed_amount do not appear anywhere.
  </action>
  <verify>
    <automated>cd canary-propos && npx tsc --noEmit && grep -c "vendor_cost\|billed_amount" src/app/\(tenant\)/my-home/page.tsx</automated>
  </verify>
  <done>/my-home shows five summary cards; tsc clean; grep returns 0 (no vendor_cost or billed_amount in file)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant → work_orders COUNT | Tenant queries work orders for their property — must be scoped to their own submissions |
| tenant → announcements COUNT | Unread count derived from public columns; no body text exposed in count query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Information Disclosure | work_orders COUNT query | mitigate | WHERE created_by = person.id — only counts the tenant's own submissions, not all property work orders |
| T-06-12 | Information Disclosure | /my-home vendor_cost exposure | mitigate | D-11 enforced — grep gate in verify step confirms 0 occurrences |
| T-06-13 | Information Disclosure | announcements unread count | accept | COUNT of records tenant already has SELECT access to; no new data exposure |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- npx tsc --noEmit exits 0
- grep -c "vendor_cost\|billed_amount" src/app/(tenant)/my-home/page.tsx returns 0
- /my-home renders five summary cards (Lease, Payments, Maintenance, Checklist, Announcements)
- TenantShell sidebar and mobile tab bar include Checklist and Announcements links
- All six TENANT-0X sub-pages are reachable from the landing
</verification>

<success_criteria>
/my-home landing shows five summary cards with live data. TenantShell has Checklist and Announcements nav items. All six TENANT requirements (TENANT-01 through TENANT-06) are wired: portal exists (01), payments reachable (02), maintenance reachable (03), lease PDF reachable (04), checklist reachable (05), announcements reachable (06). Zero vendor_cost or billed_amount references in any tenant-facing file touched by this plan.
</success_criteria>

<output>
Create `.planning/phases/06-tenant-portal/06-04-SUMMARY.md` when done
</output>
